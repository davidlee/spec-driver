# supekku.cli.admin

Admin command group for niche workspace maintenance operations.

## Constants

- `app`

## Functions

- @app.command `preboot(repo_root) -> None`: Generate static boot context for cache-optimised agent sessions.
