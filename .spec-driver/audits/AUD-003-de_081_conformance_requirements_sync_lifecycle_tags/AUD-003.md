---
id: AUD-003
slug: de_081_conformance_requirements_sync_lifecycle_tags
name: "Audit - DE-081 conformance: requirements sync, lifecycle, tags"
created: "2026-03-09"
updated: "2026-03-09"
status: completed
kind: audit
mode: conformance
delta_ref: DE-081
relations:
  - type: documents
    target: DE-081
---

## Scope

Conformance audit for DE-081 against SPEC-122, PROD-008, PROD-009, and ADR-008.

## Observations

### O1: Coverage replacement semantics (ISSUE-032)

- `_apply_coverage_blocks()` now clears all `coverage_evidence` and `coverage_entries` before rebuilding from current sources.
- `coverage_evidence` assigned from current `coverage_map`, not unioned with previous.
- Sync idempotency restored (NF-002): running sync twice produces byte-identical registry.
- Removing an artefact from a coverage block correctly removes it from evidence on next sync.
- Removing an entire coverage block clears all evidence for affected requirements.
- Status preserved when evidence is lost (ADR-008 §3 — loss of evidence is drift, not automatic downgrade).
- **Verdict**: Conforms to SPEC-122.NF-002 and ADR-008 §5.

### O2: Terminal lifecycle statuses (ISSUE-029)

- `deprecated` and `superseded` added to `VALID_STATUSES` in `lifecycle.py`.
- `TERMINAL_STATUSES` set includes `retired`, `deprecated`, `superseded`.
- Coverage-derived status skipped for terminal-status requirements — normative lifecycle claims preserved.
- SPEC-122.FR-002 updated to list all six statuses.
- **Verdict**: Conforms to SPEC-122.FR-002, ADR-008 §1 (specs own normative truth).

### O3: Inline requirement tags (ISSUE-018)

- `_REQUIREMENT_LINE` regex extended with optional `[tag1, tag2]` group.
- Tags parsed, sorted, and stored on `RequirementRecord`.
- Tags survive save/load round-trip.
- `filter(tag=...)` already existed and works correctly.
- Tags merged (union) during `RequirementRecord.merge()`.
- **Verdict**: Conforms to SPEC-122.FR-004 (extraction pattern extended).

### O4: ISSUE-033 (already fixed)

- Confirmed fixed by DE-043 (commit `62cfcd5`).
- Current code filters unknown statuses at both ingestion and derivation boundaries.
- **Verdict**: No action needed. Closed as resolved.

## Evidence

- **Tests**: 99 tests in `registry_test.py` (89 existing + 10 new), all passing.
- **Full suite**: 3573 passed, 0 failed.
- **Linters**: `ruff check` clean. `pylint` — no new warnings on changed files.
- **Verification artefacts**: VT-081-001, VT-081-002, VT-081-003 all verified.
- **Spec coverage**: SPEC-122 coverage block updated with DE-081 entries.
- **Commits**: `11868e8` (tests), `4557cc4` (artefacts).

## Recommendations

None. All objectives met. Delta ready for closure.
