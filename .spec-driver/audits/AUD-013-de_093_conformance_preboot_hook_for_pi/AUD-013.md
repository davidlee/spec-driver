---
id: AUD-013
slug: de_093_conformance_preboot_hook_for_pi
name: "Audit - DE-093 conformance: preboot hook for pi"
created: "2026-03-15"
updated: "2026-03-15"
status: completed
kind: audit
mode: conformance
delta_ref: DE-093
code_scope:
  - supekku/scripts/lib/core/preboot.py
  - supekku/scripts/install.py
  - supekku/pi.extensions/spec-driver-preboot.ts
  - flake.nix
---

## Observations

### F-001: Preboot dual output — aligned

`write_preboot_file()` writes both `.agents/spec-driver-boot.md` (Claude Code) and `.pi/APPEND_SYSTEM.md` (pi). Content-diffing avoids unnecessary writes. 17 tests pass including 3 pi-specific tests (`test_writes_pi_append_system`, `test_pi_output_matches_primary`, `test_pi_output_skips_write_when_unchanged`).

- **disposition**: `aligned`

### F-002: Pi extension — aligned

`supekku/pi.extensions/spec-driver-preboot.ts` hooks `session_shutdown`, runs `spec-driver admin preboot ctx.cwd`. ~10 LOC, well-documented with DR/DEC references.

- **disposition**: `aligned`

### F-003: Install pipeline — aligned

`_install_pi_config()` copies `supekku/pi.extensions/*` → `.pi/extensions/`. Integrated into `initialize_workspace()`. 6 unit tests + 2 integration tests pass. `.pi/APPEND_SYSTEM.md` gitignore entry added with 2 tests.

- **disposition**: `aligned`

### F-004: Nix wrapper (belt) — aligned

`flake.nix` wraps both `jailed-pi` and `jailed-pi-research` with host-side `spec-driver admin preboot "$PWD"` before jail entry. Name collision resolved (raw vs wrapper). `lib.getExe'` used correctly.

- **disposition**: `aligned`

### F-005: Lint — aligned

`pylint` scores 10.00/10 on `preboot.py` and `install.py` (errors-only check).

- **disposition**: `aligned`

### F-006: IP-093 is template boilerplate — follow_up_backlog

IP-093 was never filled in from the template. The work was straightforward enough that this didn't impede execution. Not blocking closure.

- **disposition**: `tolerated_drift`
- **rationale**: Work completed successfully without detailed IP; low ceremony appropriate for this scope.

## Evidence

- `uv run pytest supekku/scripts/lib/core/preboot_test.py` — 17/17 passed
- `uv run pytest supekku/scripts/lib/install_test.py -k pi` — 12/12 passed
- `uv run pytest -x -q` — 4068 passed, 4 skipped
- `uv run pylint ... --enable=E` — 10.00/10
- 5 commits: c9b6561, 0c16582, 6132f94, 8304e69, f89f6e5

## Recommendations

None. All deliverables implemented and tested.
