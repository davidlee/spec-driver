---
id: AUD-019
slug: de_112_conformance_pydantic_validation_and_dataclass_cleanup
name: "Audit - DE-112 conformance: Pydantic validation and dataclass cleanup"
created: "2026-03-22"
updated: "2026-03-22"
status: completed
kind: audit
mode: conformance
delta_ref: DE-112
spec_refs: [SPEC-116, SPEC-132, SPEC-159]
code_scope:
  - supekku/scripts/lib/core/frontmatter_schema.py
  - supekku/scripts/lib/validation/validator.py
---

## Observations

### Phase 1 — Model conversion

- `Relation` and `FrontmatterValidationResult` are now `BaseModel(frozen=True)`.
- Dead `.dict()` method removed (DEC-112-004). Pydantic's `.model_dump()` available if needed.
- `MappingProxyType` replaced with plain `dict` for `data` and `attributes` fields (DEC-112-005). Frozen model prevents mutation.
- All construction sites use keyword args — no positional arg breakage.
- No frozen-mutation tests existed for these classes — no test updates needed.

### Phase 2 — Kind-aware validation

- Three validation methods + three traversal methods added to `WorkspaceValidator`.
- Pattern matches existing `_validate_phase_frontmatter`: broad `Exception` catch, emit warning.
- Models are permissive by design (`extra="ignore"`, optional defaults) — only genuinely wrong types trigger warnings.
- 7 new tests cover valid/malformed for each kind + missing-directory safety.

### Phase 3 — Historical noise

- AUD-012: 11 findings fixed (`resolved` → `reconciled`).
- Legacy phases: 6 files backfilled with `kind: phase` and/or `status: completed`.
- IP-109: already canonical — no fix needed.
- Validation: 11 errors → 0, 56 warnings → 43 (remaining are audit-gate/unresolved-ref/legacy).

## Evidence

- 521 targeted tests passing (frontmatter_metadata, specs, validation, relations)
- 4521 full suite passing
- `ruff check` clean on both changed files
- `spec-driver validate`: 0 errors
- DEC verification script: all design decisions confirmed programmatically

## Findings

### F-001: All design decisions implemented correctly
- outcome: pass
- disposition:
    status: reconciled
    kind: aligned
    notes: DEC-112-001 through DEC-112-005 all verified.

### F-002: No spec patches needed
- outcome: pass
- disposition:
    status: reconciled
    kind: aligned
    notes: SPEC-116, SPEC-132, SPEC-159 unchanged — implementation is additive (new validation) or internal refactor (model conversion). No authority change.

### F-003: Validation noise materially reduced
- outcome: pass
- disposition:
    status: reconciled
    kind: aligned
    notes: Error count 11→0, warning count 56→43. Remaining warnings are pre-existing audit-gate/reference issues outside DE-112 scope.

## Recommendations

None. Delta is clean for closure.
