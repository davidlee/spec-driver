---
id: AUD-021
slug: de_108_conformance_review_cli_structured_output
name: Audit - DE-108 conformance — review CLI structured output
created: "2026-03-22"
updated: "2026-03-22"
status: completed
kind: audit
mode: conformance
delta_ref: DE-108
code_scope:
  - supekku/cli/common.py
  - supekku/cli/workflow.py
---

# AUD-021 — DE-108 Conformance Audit

## Scope

Post-implementation conformance audit for DE-108 (review CLI contract for structured machine consumption). Verifies DR-108 design decisions are correctly implemented.

## Evidence

- 208 tests passing across `common_test.py`, `workflow_review_test.py`, `workflow_test.py`, `workflow_integration_test.py`
- Lint clean (pylint E-level: 10.00/10)
- All 3 phases (P01/P02/P03) completed

## Findings

### F-001: Exit codes — aligned

- **Outcome**: `EXIT_PRECONDITION=2`, `EXIT_GUARD_VIOLATION=3` in `common.py` alongside existing 0/1
- **Disposition**: aligned
- DR-108 §4 ✓

### F-002: Envelope helpers — aligned

- **Outcome**: `cli_json_success()`, `cli_json_error()`, `emit_json_and_exit()` — CLI-generic naming per DEC-108-001
- **Disposition**: aligned
- DR-108 §5 ✓

### F-003: review prime JSON — aligned

- **Outcome**: `--format json` with action enum (created/rebuilt/refreshed), full 40-char SHA, bootstrap_status, judgment_status, review_round
- **Disposition**: aligned
- DR-108 §6.1, DEC-108-002, DEC-108-006 ✓

### F-004: review complete JSON — aligned

- **Outcome**: `--format json` with outcome enum, state transition, guard violation exit code 3, teardown flag
- **Disposition**: aligned
- DR-108 §6.2 ✓

### F-005: review teardown JSON — aligned

- **Outcome**: `--format json` with removed files list
- **Disposition**: aligned
- DR-108 §6.3 ✓

### F-006: workflow status JSON — aligned

- **Outcome**: `--format json` with bootstrap_status, judgment_status, findings_summary, staleness inputs (derivation inputs only, not derived conclusion)
- **Disposition**: aligned
- DR-108 §6.4 ✓

### F-007: Finding disposition JSON — aligned

- **Outcome**: All 4 existing commands (resolve/defer/waive/supersede) accept `--format json` via shared `_disposition_finding()`, return previous/new status
- **Disposition**: aligned
- DR-108 §6.5 ✓

### F-008: Finding list — aligned

- **Outcome**: New `review finding list` command with `--round N` filter, JSON + human output
- **Disposition**: aligned
- DR-108 §6.5, DEC-108-005 ✓

### F-009: Finding not found error — aligned

- **Outcome**: Non-existent finding ID → EXIT_PRECONDITION (2) with structured error
- **Disposition**: aligned
- DEC-108-012 ✓

### F-010: Stderr suppression — aligned

- **Outcome**: JSON mode routes all errors through envelope; no stderr in JSON mode
- **Disposition**: aligned
- DEC-108-010 ✓

### F-011: Human output preserved — aligned

- **Outcome**: Default output (no `--format`) unchanged; all 44 pre-existing tests pass
- **Disposition**: aligned

### F-012: DR reconciliation — aligned

- **Outcome**: DR-108 was reconciled when implementation revealed existing finding subgroup and IO helpers. DEC-108-003, DEC-108-004, DEC-108-011 updated to reflect actual surface
- **Disposition**: aligned

### F-013: Memory update needed — follow_up_backlog

- **Outcome**: `mem.reference.workflow-commands` should be updated to reflect new `--format json` capability on all review/workflow commands and the new `finding list` command
- **Disposition**: follow_up_backlog
- Note: Will update memory as part of closure

## Summary

All 13 findings dispositioned. 12 aligned, 1 follow-up (memory update). No blocking findings. No spec patches or revisions required — this is additive feature work on existing commands.
