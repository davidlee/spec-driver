---
id: DR-057
slug: backlogregistry_class_normalisation
name: Design Revision - BacklogRegistry class normalisation and backlog subsystem improvements
created: '2026-03-07'
updated: '2026-03-07'
status: draft
kind: design_revision
aliases: []
owners: []
relations:
- type: implements
  target: DE-057
delta_ref: DE-057
source_context:
  - DE-050 (normalised registry API — completed, ADR-009 adopted)
  - DE-053 (TUI artifact browser — DEC-053-08 deferred backlog normalisation)
  - Preflight research 2026-03-07 (notes.md)
code_impacts:
  - path: supekku/scripts/lib/backlog/registry.py
    action: modify
    description: Add BacklogRegistry class wrapping existing functions; ADR-009 surface
  - path: supekku/scripts/lib/backlog/models.py
    action: modify
    description: Add per-kind status enums with validation
  - path: supekku/scripts/lib/core/artifact_view.py
    action: modify
    description: Remove _collect_backlog() shim and _load_type() special-case; add factory entry
  - path: supekku/cli/common.py
    action: modify
    description: Simplify _resolve_backlog() and _find_backlog() to use BacklogRegistry
  - path: supekku/cli/list.py
    action: modify
    description: Use BacklogRegistry for list_backlog and per-kind list commands
  - path: supekku/cli/sync.py
    action: modify
    description: Thread dry_run through _sync_backlog() and sync_backlog_registry()
  - path: supekku/cli/create.py
    action: modify
    description: Fix --from-backlog greedy flag; use BacklogRegistry.find()
  - path: supekku/cli/resolve.py
    action: modify
    description: Add _collect_backlog_items() to _build_artifact_index()
verification_alignment:
  - artefact: VT-057-registry
    kind: VT
    status: planned
    notes: Unit tests for BacklogRegistry.find/collect/iter/filter with kind and status filtering
  - artefact: VT-057-status-enums
    kind: VT
    status: planned
    notes: Per-kind status enum validation — valid values accepted, invalid rejected, default statuses correct
  - artefact: VT-057-artifact-view
    kind: VT
    status: planned
    notes: artifact_view.py uses standard factory pattern for backlog — shim removed, no special-case
  - artefact: VT-057-sync-dryrun
    kind: VT
    status: planned
    notes: _sync_backlog() with dry_run=True performs no writes
  - artefact: VT-057-link-resolver
    kind: VT
    status: planned
    notes: Link resolver resolves backlog item wikilinks (ISSUE-*, PROB-*, IMPR-*, RISK-*)
  - artefact: VT-057-from-backlog
    kind: VT
    status: planned
    notes: --from-backlog does not consume subsequent flags
  - artefact: VT-057-regression
    kind: VT
    status: planned
    notes: Existing backlog CLI and TUI tests continue to pass unchanged
design_decisions:
  - id: DEC-057-01
    summary: Single BacklogRegistry class with kind as domain-specific filter parameter
    status: decided
  - id: DEC-057-02
    summary: Per-kind status sets as frozenset constants, not Enum classes
    status: decided
  - id: DEC-057-03
    summary: Priority ordering stays external to registry class
    status: decided
  - id: DEC-057-04
    summary: Module-level functions preserved as thin wrappers for transition
    status: decided
  - id: DEC-057-05
    summary: collect() returns full dict; kind filtering via iter/filter only
    status: decided
  - id: DEC-057-06
    summary: Link resolver uses BacklogRegistry.collect() to populate artifact index
    status: decided
  - id: DEC-057-07
    summary: --from-backlog fix via eager=False and ID-format validation
    status: decided
open_questions:
  - text: Should status validation be strict (reject unknown) or permissive (warn on unknown)?
    recommendation: Permissive with warning — strict would break existing items with ad-hoc statuses
    status: open
---

# DR-057 – BacklogRegistry class normalisation and backlog subsystem improvements

## 1. Executive Summary

- **Delta**: [DE-057](./DE-057.md)
- **Status**: draft
- **Last Updated**: 2026-03-07
- **Synopsis**: Normalise the backlog subsystem from function-based API to a
  class-based `BacklogRegistry` conforming to ADR-009, define per-kind status
  enums, and fix operational gaps (dry-run, link resolution, greedy flag).

## 2. Problem & Constraints

- **Current Behaviour**: Backlog uses module-level functions
  (`discover_backlog_items()`, `find_item()`, `find_backlog_items_by_id()`)
  instead of a class with `collect/find/iter/filter`. This forces every
  consumer to adapt: `artifact_view.py` has a 30-line `_collect_backlog()`
  shim duplicating error isolation, `_load_type()` has a backlog special-case,
  `cli/common.py` has bespoke resolver functions, and `cli/list.py` calls
  discovery functions directly. Status values are undefined — 14 distinct
  values observed in the wild with no validation. Sync doesn't respect
  dry-run. Link resolution ignores backlog items. `--from-backlog` swallows
  subsequent flags.
- **Drivers / Inputs**: ADR-009 (standard registry API), DE-050 Tier 3
  (deferred), DE-053 exit criteria (DEC-053-08), ISSUE-009, -026, -034, -043
- **Constraints / Guardrails**:
  - ADR-009 governs the API surface (find/collect/iter/filter, keyword-only
    root constructor)
  - ADR-002: no backlinks in frontmatter
  - Existing module-level functions must keep working during transition
  - Priority ordering (`priority.py`) remains a separate concern
  - `just` must pass (tests + both linters)
- **Out of Scope**: Backlog speciation into separate ArtifactTypes,
  requirements sync (ISSUE-016), prioritize UX (IMPR-010), Registry
  Protocol/ABC

## 3. Architecture Intent

- **Target Outcomes**:
  - `BacklogRegistry` is a first-class ADR-009-conformant registry
  - `artifact_view.py` treats backlog identically to all other types — no
    shim, no special-case
  - Per-kind status values are defined and validated
  - All 4 operational gaps fixed (ISSUE-009, -026, -034, -043)
- **Guiding Principles**:
  - Mirror the DecisionRegistry pattern — same constructor shape, same
    method signatures, domain-specific params where ADR-009 allows
  - Keep the class thin — wrap existing discovery/lookup functions, don't
    reimplement
  - Status validation is permissive (warn on unknown) rather than strict
    (reject), to avoid breaking existing items with ad-hoc statuses
- **State Transitions / Lifecycle Impact**: Status enum definitions codify
  the per-kind lifecycle but do not change any existing item's status value

## 4. Code Impact Summary

| Path | Current State | Target State |
| --- | --- | --- |
| `supekku/scripts/lib/backlog/registry.py` | Module-level functions only | Add `BacklogRegistry` class; existing functions become thin wrappers |
| `supekku/scripts/lib/backlog/models.py` | `BacklogItem` dataclass, no status validation | Add per-kind status sets + `is_valid_status()` helper |
| `supekku/scripts/lib/core/artifact_view.py` | `_collect_backlog()` shim + `_load_type()` special-case | Remove both; add `_make_backlog_registry` factory to `_REGISTRY_FACTORIES` |
| `supekku/cli/common.py` | `_resolve_backlog()` calls `find_backlog_items_by_id()` | Calls `BacklogRegistry(root).find(id)` |
| `supekku/cli/list.py` | `list_backlog()` calls `discover_backlog_items()` directly | Uses `BacklogRegistry(root).filter(kind=..., status=...)` |
| `supekku/cli/sync.py` | `_sync_backlog()` ignores `dry_run` | Accepts and threads `dry_run` param |
| `supekku/cli/create.py` | `--from-backlog` is greedy `typer.Option` | Add ID-format validation; use `BacklogRegistry.find()` |
| `supekku/cli/resolve.py` | `_build_artifact_index()` omits backlog | Add `_collect_backlog_items()` populating ISSUE/PROB/IMPR/RISK entries |

## 5. Verification Alignment

| Verification | Kind | Impact | Notes |
| --- | --- | --- | --- |
| VT-057-registry | VT | new | BacklogRegistry.find/collect/iter/filter — kind filtering, status filtering, empty corpus, missing items |
| VT-057-status-enums | VT | new | Per-kind status sets — valid values accepted, unknown values warned, default statuses match TEMPLATES |
| VT-057-artifact-view | VT | new | artifact_view.py factory pattern — BacklogRegistry in _REGISTRY_FACTORIES, no _collect_backlog, no special-case |
| VT-057-sync-dryrun | VT | new | _sync_backlog(dry_run=True) performs no filesystem writes |
| VT-057-link-resolver | VT | new | Wikilinks [[ISSUE-016]], [[PROB-002]], [[IMPR-009]], [[RISK-001]] resolve to correct paths |
| VT-057-from-backlog | VT | new | --from-backlog ISSUE-001 --spec SPEC-001 does not consume --spec as the backlog ID |
| VT-057-regression | VT | regression | Existing backlog tests in registry_test.py, priority_test.py, backlog_formatters_test.py, list_test.py, create_test.py unchanged |

## 6. Supporting Context

- **Research**: DE-050 research.md — full registry audit matrix across 9
  registries; DE-057 preflight notes — backlog-specific findings
- **Related Deltas**: DE-050 (registry normalisation — completed), DE-053
  (TUI — in-progress, carries disposable shim)
- **ADR-009**: Governs the API surface; explicitly allows domain-specific
  filter params and defers Protocol/ABC

## 7. Design Decisions & Trade-offs

### DEC-057-01: Single BacklogRegistry class with kind as domain-specific filter (decided)

**Context**: 4 backlog item types (issue, problem, improvement, risk) share
a single `BacklogItem` model. Options: (A) one registry with `kind` filter,
(B) 4 separate registries, (C) hybrid with sub-type awareness in ArtifactType.

**Decision**: Option A. Single `BacklogRegistry` with `kind` as a
domain-specific filter parameter on `iter()` and `filter()`.

**Rationale**: Items share a unified model. Priority ordering operates across
types. The primary interaction surface is the cross-type prioritised backlog.
4 registries = 4x boilerplate, 4 ArtifactType values, 4 factory entries —
for marginal gain. `list issues` = `filter(kind="issue")`.

**Consequence**: `ArtifactType.BACKLOG` remains a single enum value. TUI
shows "Backlog (57)". Per-kind views are a filter, not a type distinction.

### DEC-057-02: Per-kind status sets as frozenset constants (decided)

**Context**: 14 distinct status values observed in the wild. Need validation
without breaking existing items.

**Decision**: Define per-kind status sets as `frozenset` constants in
`backlog/models.py`:

```python
ISSUE_STATUSES: frozenset[str] = frozenset({
  "open", "triaged", "in-progress", "resolved", "done",
  "implemented", "verified",
})
PROBLEM_STATUSES: frozenset[str] = frozenset({
  "captured", "investigating", "mitigated", "resolved",
})
IMPROVEMENT_STATUSES: frozenset[str] = frozenset({
  "idea", "planned", "in-progress", "implemented",
})
RISK_STATUSES: frozenset[str] = frozenset({
  "suspected", "confirmed", "mitigated", "accepted", "expired",
})

BACKLOG_STATUSES: dict[str, frozenset[str]] = {
  "issue": ISSUE_STATUSES,
  "problem": PROBLEM_STATUSES,
  "improvement": IMPROVEMENT_STATUSES,
  "risk": RISK_STATUSES,
}

# Statuses that indicate closure (excluded from default list views)
TERMINAL_STATUSES: frozenset[str] = frozenset({
  "resolved", "done", "implemented", "verified",
  "mitigated", "accepted", "expired",
})
```

**Rationale**: `frozenset` over `Enum` — simpler, no import ceremony, easy
to check membership. Per-kind sets capture the different lifecycles. A single
`TERMINAL_STATUSES` set replaces the hardcoded `["resolved", "implemented"]`
filter in `list_backlog()`. Values derived from TEMPLATES defaults + observed
frontmatter values.

**Open question**: Should validation be strict (reject unknown) or permissive
(warn on unknown)? Recommendation: permissive with `logger.warning()` — the
wild contains `draft`, `stub`, `adjudicated` which may be intentional
one-offs.

**Consequence**: `list_backlog()`'s status exclusion becomes
`if i.status in TERMINAL_STATUSES` instead of a hardcoded list. IMPR-010
(status-aware checkboxes) can consume these sets directly.

### DEC-057-03: Priority ordering stays external to registry class (decided)

**Context**: `priority.py` (360 lines) handles ordering, editor integration,
head-tail partitioning, and `backlog.yaml` persistence. Should this move
into `BacklogRegistry`?

**Decision**: No. Priority ordering remains in `priority.py` as a separate
concern. `BacklogRegistry` handles discovery and lookup. `priority.py`
handles ordering and persistence.

**Rationale**: The registry pattern (ADR-009) is about artifact
discovery/lookup, not presentation ordering. Mixing them would make
`BacklogRegistry` the only registry with ordering semantics — a design
outlier. The two modules compose cleanly: `BacklogRegistry.filter()` returns
items, `sort_by_priority()` orders them.

### DEC-057-04: Module-level functions preserved as thin wrappers (decided)

**Context**: Existing callers use `discover_backlog_items()`, `find_item()`,
etc. directly.

**Decision**: Keep module-level functions as thin wrappers delegating to
`BacklogRegistry` methods. No deprecation warnings in this delta — evaluate
removal in a future cleanup.

```python
def discover_backlog_items(*, root=None, kind="all"):
  """Wrapper for backwards compatibility."""
  registry = BacklogRegistry(root=root)
  kind_filter = kind if kind != "all" else None
  return list(registry.iter(kind=kind_filter))
```

**Rationale**: Minimises blast radius. Callers migrate at their own pace.
The wrappers are trivial and disposable. Breaking all callers simultaneously
is unnecessary risk for zero user-facing benefit.

### DEC-057-05: collect() returns full dict; kind filtering via iter/filter (decided)

**Context**: ADR-009 specifies `collect() -> dict[str, Record]`. DE-057 wants
kind-scoped collection. Options: (A) `collect(kind=None)` adding a parameter,
(B) `collect()` always returns all, kind filtering via `iter()`/`filter()`.

**Decision**: Option B. `collect()` returns all items (dict keyed by ID).
Kind filtering is via `iter(kind=None, status=None)` and
`filter(kind=None, status=None, ...)`.

**Rationale**: ADR-009's `collect()` contract is "return complete dict." Adding
parameters diverges from the convention. The `artifact_view.py` layer calls
`collect()` and expects the full corpus. Kind-scoped access is a filter
concern, not a collection concern. This matches how DecisionRegistry's
`collect()` returns all decisions regardless of status.

**Consequence**: `iter()` and `filter()` gain `kind` as a domain-specific
parameter — consistent with ADR-009's allowance for domain-specific filter
params.

### DEC-057-06: Link resolver uses BacklogRegistry to populate artifact index (decided)

**Context**: `resolve.py:_build_artifact_index()` builds an
`ArtifactIndex = dict[str, tuple[Path, str]]` by calling per-type collection
functions. Backlog is missing (ISSUE-034).

**Decision**: Add `_collect_backlog_items()` to `resolve.py` that instantiates
`BacklogRegistry` and populates the index with entries keyed by ID (ISSUE-016,
PROB-002, etc.) mapped to `(item.path, item.kind)`.

```python
def _collect_backlog_items(root: Path, index: ArtifactIndex) -> None:
  from supekku.scripts.lib.backlog.registry import BacklogRegistry
  registry = BacklogRegistry(root=root)
  for item_id, item in registry.collect().items():
    index[item_id] = (item.path.relative_to(root), item.kind)
```

**Rationale**: Follows the exact pattern of `_collect_decisions()`,
`_collect_specs()`, etc. already in `resolve.py`. Simple, consistent.

### DEC-057-07: --from-backlog fix via eager=False and ID validation (decided)

**Context**: ISSUE-043 — `--from-backlog --help` consumes `--help` as the
backlog item ID because Typer/Click treats the option value greedily.

**Decision**: Two changes:
1. Ensure the option is not eager (`is_eager=False`, which is the default —
   verify it's not set to `True`)
2. Add a callback that validates the value looks like a backlog item ID
   (`re.match(r'^(ISSUE|PROB|IMPR|RISK)-\d+$', value)`) and raises
   `typer.BadParameter` if not

```python
def _validate_backlog_id(value: str | None) -> str | None:
  if value is None:
    return None
  if not re.match(r"^(ISSUE|PROB|IMPR|RISK)-\d+$", value):
    raise typer.BadParameter(
      f"'{value}' does not look like a backlog item ID "
      "(expected ISSUE-NNN, PROB-NNN, IMPR-NNN, or RISK-NNN)"
    )
  return value
```

**Rationale**: The root cause is that Click can't distinguish between
`--from-backlog`'s value and a subsequent flag when the value is arbitrary
text. Validation rejects non-ID values early, making the error clear. Also
replaces the `discover_backlog_items()` call with `BacklogRegistry.find()`.

## 8. Open Questions

- [ ] **Status validation strictness** — strict (reject unknown) vs permissive
  (warn on unknown). Recommendation: permissive. Wild contains `draft`,
  `stub`, `adjudicated` which may be intentional. Strict rejection would
  break `discover_backlog_items()` for existing items. Decide during Phase 1
  implementation.

## 9. Rollout & Operational Notes

- **Migration**: None required. Module-level wrapper functions preserve
  backwards compatibility. No data format changes.
- **Rollback**: Revert the commit. No state changes to artifacts.
- **Exit criteria**: All operational issues (ISSUE-009, -026, -034, -043)
  verified and closeable. `artifact_view.py` shim removed. `just` passes.

## 10. References & Links

- ADR-009: `.spec-driver/decisions/ADR-009-standard_registry_api_convention.md`
- DE-050 research: `.spec-driver/deltas/DE-050-*/research.md`
- DE-053 DEC-053-08: BacklogRegistry shim decision
- Existing code:
  - `supekku/scripts/lib/backlog/registry.py` (536 lines)
  - `supekku/scripts/lib/backlog/models.py` (28 lines)
  - `supekku/scripts/lib/backlog/priority.py` (360 lines)
  - `supekku/scripts/lib/core/artifact_view.py` (439 lines)
  - `supekku/cli/resolve.py:_build_artifact_index()` (lines 132-149)
