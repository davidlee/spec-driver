---
id: IP-063
slug: cli_interface_fixes_id_inference_list_truncation_bugs_flag_parsing
name: 'Implementation Plan - CLI interface fixes: ID inference, list truncation bugs'
created: '2026-03-08'
updated: '2026-03-08'
status: draft
kind: plan
aliases: []
---

```yaml supekku:plan.overview@v1
schema: supekku.plan.overview
version: 1
plan: IP-063
delta: DE-063
revision_links:
  aligns_with: []
specs:
  primary: []
  collaborators: []
requirements:
  targets: []
  dependencies: []
phases:
  - id: IP-063.PHASE-01
  - id: IP-063.PHASE-02
```

```yaml supekku:verification.coverage@v1
schema: supekku.verification.coverage
version: 1
subject: IP-063
entries:
  - artefact: VT-063-01
    kind: VT
    requirement: ''
    status: planned
    notes: resolve_by_id unit tests (prefixed, numeric, ambiguous, unknown)
  - artefact: VT-063-02
    kind: VT
    requirement: ''
    status: planned
    notes: InferringGroup integration tests via CliRunner
  - artefact: VT-063-03
    kind: VT
    requirement: ''
    status: planned
    notes: add_row_with_truncation markup-aware truncation tests
```

## 1. Summary
- **Delta**: DE-063 — CLI interface fixes: ID inference, list truncation bugs
- **Design Revision**: [DR-063](./DR-063.md)
- **Issues**: ISSUE-045 (ID inference), ISSUE-038 (memory truncation),
  ISSUE-041 (spec truncation), ISSUE-042 (flag parsing — wontfix)
- **Desired Outcome**: `show`/`view` accept bare IDs; `list --truncate`
  renders correct status values

## 2. Context & Constraints
- **Current Behaviour**: `show`/`view` require explicit entity subcommand;
  `list --truncate` corrupts Rich markup in status column
- **Target Behaviour**: Per DR-063 §4a (ID inference) and §4b (truncation fix)
- **Dependencies**: None
- **Constraints**: Additive only for ID inference; no `prepare_row()` contract
  changes for truncation

## 3. Gate Check
- [x] Backlog items linked (ISSUE-045, ISSUE-038, ISSUE-041, ISSUE-042)
- [x] DR-063 approved after adversarial review
- [x] Test strategy identified (unit + CliRunner integration)
- [x] No workspace/config changes needed

## 4. Phase Overview

| Phase | Objective | Entrance Criteria | Exit Criteria | Phase Sheet |
| --- | --- | --- | --- | --- |
| P01 — Truncation fix | Fix markup-aware truncation in `table_utils.py` | IP approved | `add_row_with_truncation` uses `Text.from_markup()`, dead `truncate_text()` removed, VT-063-03 passing, lint clean | `phases/phase-01.md` |
| P02 — ID inference | Add type-inferring resolution to `show`/`view` | P01 complete | `InferringGroup` + `resolve_by_id()` + hidden "inferred" command working, VT-063-01 + VT-063-02 passing, lint clean | `phases/phase-02.md` |

Rationale for ordering: P01 is smaller and independent — ship the bug fix
first. P02 is the feature work, depends on `resolve.py` changes.

## 5. Phase Detail Snapshot
- **Design Revision**: [DR-063](./DR-063.md)
- **Notes**: [notes.md](./notes.md)
- **Active Phase Sheet**: `phases/phase-01.md`

## 6. Testing & Verification Plan
- **VT-063-01**: `resolve_by_id()` unit tests — prefixed ID, numeric ID,
  ambiguous numeric, unknown, card ID, out-of-scope types
- **VT-063-02**: `InferringGroup` integration tests via `CliRunner` —
  subcommand passthrough, bare prefixed ID, bare numeric, disambiguation,
  unknown error, no-args help, output flags
- **VT-063-03**: `add_row_with_truncation()` tests — plain text, short markup
  (no truncation), long markup (truncation preserves style), `max_width <= 3`,
  mixed markup/plain row
- **Rollback**: Revert commits. No persistent state.

## 7. Risks & Mitigations

| Risk | Mitigation | Owner |
| --- | --- | --- |
| `Text.from_markup()` edge cases with malformed markup | Rich treats unrecognized tags as literal text; no crash risk | Dev |
| `InferringGroup` interaction with Typer internals | Integration tests via CliRunner validate full chain | Dev |
| `_build_artifact_index()` perf for large repos | Single scan, same cost as existing `resolve links`; acceptable for CLI | Dev |

## 8. Open Questions & Decisions
All resolved — see DR-063 §7 and §8.

## 9. Progress Tracking
- [ ] P01 complete (truncation fix)
- [ ] P02 complete (ID inference)
- [ ] ISSUE-042 closed as wontfix
- [ ] Verification gates passed
- [ ] Delta closed

## 10. Notes / Links
- DR-063 adversarial review: 11 findings accepted, 3 documented as out-of-scope
