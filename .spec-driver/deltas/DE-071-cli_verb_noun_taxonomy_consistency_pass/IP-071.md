---
id: IP-071
slug: cli_verb_noun_taxonomy_consistency_pass
name: 'Implementation Plan - CLI verb-noun taxonomy: consistency pass'
created: '2026-03-08'
updated: '2026-03-09'
status: draft
kind: plan
aliases: []
---

```yaml supekku:plan.overview@v1
schema: supekku.plan.overview
version: 1
plan: IP-071
delta: DE-071
revision_links:
  aligns_with:
  - DR-071
specs:
  primary:
  - SPEC-110
  collaborators:
  - PROD-010
requirements:
  targets: []
  dependencies: []
phases:
- id: IP-071.PHASE-01
- id: IP-071.PHASE-02
- id: IP-071.PHASE-03
```

```yaml supekku:verification.coverage@v1
schema: supekku.verification.coverage
version: 1
subject: IP-071
entries:
  - artefact: VT-071-01
    kind: VT
    requirement: ''
    status: planned
    notes: CLI integration tests for admin group commands
  - artefact: VT-071-02
    kind: VT
    requirement: ''
    status: planned
    notes: CLI integration tests for show/list schema
  - artefact: VT-071-03
    kind: VT
    requirement: ''
    status: planned
    notes: Existing schema tests adapted to new invocation paths
  - artefact: VT-071-04
    kind: VT
    requirement: ''
    status: planned
    notes: Existing skills tests removed; install feedback tested
  - artefact: VA-071-01
    kind: VA
    requirement: ''
    status: planned
    notes: Grep sweep confirms no stale command references in guidance layer
```

## 1. Summary

- **Delta**: DE-071 – CLI verb-noun taxonomy: consistency pass
- **Specs Impacted**: SPEC-110, PROD-010
- **Issues**: ISSUE-030
- **Desired Outcome**: All CLI commands follow VERB NOUN grammar. Niche
  maintenance commands grouped under `admin`. Redundant `skills` group removed.
  Agent guidance updated.

## 2. Context & Constraints

- **Current Behaviour**: `skills sync` (NOUN VERB, redundant), `schema list/show`
  (NOUN VERB), `compact/resolve/backfill` (niche commands cluttering top level)
- **Target Behaviour**: Per DR-071 — consistent VERB NOUN, `admin` group, schema
  under `show`/`list`, `skills` removed with install feedback
- **Dependencies**: None
- **Constraints**: No backward-compat aliases needed. Agent guidance is the
  migration path. `sync` UX unchanged.

## 3. Gate Check

- [x] Backlog items linked (ISSUE-030)
- [x] Design revision complete (DR-071)
- [x] Test strategy identified (adapt existing + new integration tests)
- [x] No workspace/config changes needed

## 4. Phase Overview

| Phase | Objective | Entry | Exit |
|-------|-----------|-------|------|
| P01 — CLI restructure | Move/remove/group commands, update tests | DR-071 accepted | All new command paths work, all tests pass, linters clean |
| P02 — Install feedback | Surface skill-change output in `install` | P01 complete | `install` reports skill changes, tests pass |
| P03 — Guidance sweep | Update memories, skills, docs | P02 complete | No stale command references in guidance layer (VA-071-01) |

## 5. Phase Detail Snapshot

- **Design Revision**: [DR-071](./DR-071.md)
- **Active Phase Sheet**: (created below)
- **Parallelisable Work**: P01 has three independent sub-tasks (schema move,
  admin group, skills removal) that can be done in any order.

## 6. Testing & Verification Plan

- **Adapted**: `schema_test.py` → test via `show schema`/`list schemas`
- **Removed**: `skills_test.py`
- **New**: `admin_test.py` — admin group routing tests
- **New**: Install skill-feedback test
- **VA-071-01**: Grep sweep for stale references

## 7. Risks & Mitigations

| Risk | Mitigation |
|------|------------|
| Stale agent guidance causes agent confusion | P03 is an explicit phase gate |
| Internal imports of `skills.app` or `schema.app` | Grep before removing; tests catch |

## 8. Open Questions & Decisions

None remaining.

## 9. Progress Tracking

- [ ] P01 — CLI restructure
- [ ] P02 — Install feedback
- [ ] P03 — Guidance sweep
- [ ] All verification gates passed
