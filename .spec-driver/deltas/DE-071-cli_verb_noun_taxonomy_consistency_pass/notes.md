# Notes for DE-071

## Phase 1 — CLI Restructure (complete)

### Changes made

1. **Schema moved into show/list**
   - `schema.py`: removed Typer `app`, now a library module exporting `list_schemas()` and `show_schema()`
   - `show.py`: added `show schema` subcommand delegating to `schema.show_schema()`
   - `list.py`: added `list schemas` subcommand delegating to `schema.list_schemas()`; singular alias `list schema` registered

2. **Admin group created**
   - New `admin.py`: thin Typer group mounting `compact.app`, `resolve.app`, `backfill.app`
   - Commands: `admin compact delta`, `admin resolve links`, `admin backfill spec`

3. **Skills group removed**
   - Deleted `skills.py`, `skills_test.py`
   - `install` already calls `sync_skills()`; now surfaces result (phase 2)

4. **main.py updated**
   - Removed: `schema`, `skills`, `compact`, `resolve`, `backfill` registrations
   - Added: `admin` group registration

5. **Tests adapted**
   - `schema_test.py`: rewired to invoke via `show schema` / `list schemas` through main app
   - `compact_test.py`: updated invocations to `["admin", "compact", "delta", ...]`
   - New `admin_test.py`: routing tests for admin group + verification old commands removed

### Verification

- 566 tests pass (cli/), 5 skipped, 1 pre-existing failure deselected
- ruff: zero warnings on changed files
- pylint: admin.py scores 10/10

## Phase 2 — Install Feedback (complete)

- `install.py`: captured `sync_skills()` return value, added `_print_skills_summary()`
- Output: "Skills: up to date" or "Skills: installed N" or "Skills: installed N, pruned M"
- Verified: `spec-driver install --yes` shows "Skills: installed 4"

## Phase 3 — Guidance Sweep (complete)

### Updated files

**Package skill sources** (canonical):

- `supekku/skills/spec-driver/SKILL.md`: `schema list/show` → `list schemas` / `show schema`
- `supekku/skills/reviewing-memory/SKILL.md`: `resolve links` → `admin resolve links`
- `supekku/skills/maintaining-memory/SKILL.md`: `resolve links` → `admin resolve links`
- `supekku/skills/capturing-memory/SKILL.md`: `resolve links` → `admin resolve links`

**Memory records**:

- `mem.signpost.spec-driver.skill-authoring.md`: `skills sync` → `install`
- `mem.pattern.dr-authoring-review-loop.md`: `skills sync` → `install`
- `mem.pattern.spec-driver.frontmatter-compaction.md`: `compact delta` → `admin compact delta`

**Backlog**:

- `ISSUE-034.md`: `resolve links` → `admin resolve links`

**Installed skills** (derived, synced via install):

- `.spec-driver/skills/` — regenerated by `spec-driver install`

### Not updated (historical records)

- `.spec-driver/registry/deltas.yaml` — contains historical verification entries for completed deltas; updating would cause churn in a large generated file for no functional benefit
- `.spec-driver/deltas/DE-005/`, `DE-033/`, `DE-036/`, `DE-055/` notes/phases — historical implementation records that agents don't use for command discovery

### VA-071-01 result

Zero stale command references in skills and memory (the agent-facing guidance layer).
