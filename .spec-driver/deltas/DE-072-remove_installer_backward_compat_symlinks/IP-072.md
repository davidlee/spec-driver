---
id: IP-072
slug: remove_installer_backward_compat_symlinks
name: Implementation Plan - remove installer backward-compat symlinks
created: '2026-03-08'
updated: '2026-03-08'
status: draft
kind: plan
aliases: []
---

```yaml supekku:plan.overview@v1
schema: supekku.plan.overview
version: 1
plan: IP-072
delta: DE-072
revision_links:
  aligns_with: []
specs:
  primary:
  - SPEC-129
  collaborators: []
requirements:
  targets: []
  dependencies: []
phases:
- id: IP-072-P01
- id: IP-072.PHASE-01
```

```yaml supekku:verification.coverage@v1
schema: supekku.verification.coverage
version: 1
subject: IP-072
entries:
  - artefact: installer pytest coverage
    kind: VT
    requirement: SPEC-129.FR-001
    status: planned
    notes: Verify fresh installs create canonical `.spec-driver/` directories and do not create compatibility symlinks.
```

## 1. Summary
- **Delta**: DE-072 - remove installer backward-compat symlinks
- **Specs Impacted**: SPEC-129
- **Desired Outcome**: Installer behavior and tests align on canonical-only workspace creation.
- **Documentation Reconciliation**: Update ADR-006 so the recorded workspace-install behavior matches the code change in this delta.

## 2. Context & Constraints
- **Current Behaviour**: Fresh installs create compatibility paths such as `specify/`, `change/`, `backlog/`, and `memory/` in addition to canonical directories under `.spec-driver/`.
- **Target Behaviour**: Fresh installs stop creating those compatibility paths while retaining canonical workspace initialization.
- **Dependencies**: Installer code and installer tests only.
- **Constraints**: Keep the delta narrow and preserve existing canonical workspace creation.

## 3. Gate Check
- [x] Delta specifies required change scope
- [x] Test strategy identified (targeted pytest coverage for installer behavior)
- [x] Workspace/config changes assessed

## 4. Phase Overview
| Phase | Objective | Entrance Criteria | Exit Criteria / Done When | Phase Sheet |
| --- | --- | --- | --- | --- |
| Phase 1 - Remove installer compatibility views | Update installer code, tests, and ADR-006 references for canonical-only workspace creation | DE/DR/IP reviewed, affected files identified | Code/tests/docs updated; targeted verification passing | `phases/phase-01.md` |

## 5. Phase Detail Snapshot
- **Research Notes**: `DE-072/notes.md`
- **Design Revision**: `DR-072.md`
- **Active Phase Sheet**: `phases/phase-01.md` once created
- **Plan Updates**: Update this plan if verification scope or touched files expand.

## 6. Testing & Verification Plan
- **Updated Suites**: Targeted installer pytest coverage.
- **New Cases**: Fresh install should not create `specify/`, `change/`, `backlog/`, or `memory/` paths while canonical directories still exist.
- **Tooling/Fixtures**: Existing installer test fixtures.
- **Rollback Plan**: Reintroduce compatibility creation in the installer if required.
- **Verification Coverage**: Use the coverage entry above as the concrete gate for this delta.

## 7. Risks & Mitigations
| Risk | Mitigation | Owner |
| --- | --- | --- |
| Tests or messages retain stale symlink assumptions | Update them in the same patch and verify explicitly | Agent |

## 8. Open Questions & Decisions
- [ ] Keep scope bounded to installer behavior only.

## 9. Progress Tracking
- [ ] Phase 1 complete
- [ ] Verification gates passed

## 10. Notes / Links
- ADR-006
- `supekku/scripts/install.py`
