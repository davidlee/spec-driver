---
id: IP-094
slug: pi_extension_artifact_event_tracking_via_tool_result_hooks
name: 'Implementation Plan - pi extension: artifact event tracking via tool_result hooks'
created: '2026-03-14'
updated: '2026-03-14'
status: draft
kind: plan
aliases: []
---

```yaml supekku:plan.overview@v1
schema: supekku.plan.overview
version: 1
plan: IP-094
delta: DE-094
revision_links:
  aligns_with:
    - DR-094
specs:
  primary: []
  collaborators: []
requirements:
  targets: []
  dependencies: []
phases:
  - id: IP-094-P01
  - id: IP-094.PHASE-01
```

## 1. Summary

- **Delta**: DE-094 — pi extension: artifact event tracking via tool_result hooks
- **Prior art**: DE-060 (`artifact_event.py`), DE-093 (pi extension install pipeline)
- **Desired Outcome**: pi sessions emit the same v1 artifact events as Claude Code,
  enabling TUI real-time tracking

## 2. Context & Constraints

- **Current Behaviour**: pi sessions produce no artifact events; TUI cannot track them
- **Target Behaviour**: `tool_result` events for read/edit/write classified and emitted
  to `.spec-driver/run/events.jsonl` + `tui.sock`
- **Dependencies**: None — install pipeline (DE-093) already handles `pi.extensions/`
- **Constraints**: Fail-silent; must not interfere with pi operation

## 3. Gate Check

- [x] Design revision complete (DR-094)
- [x] Install pipeline exists (DE-093)
- [ ] Test strategy: unit tests for classification + event schema + JSONL write

## 4. Phase Overview

| Phase                    | Objective                    | Entrance Criteria | Exit Criteria                      |
| ------------------------ | ---------------------------- | ----------------- | ---------------------------------- |
| P01 — Implement + verify | Write extension, tests, lint | DR-094 accepted   | Extension + tests pass, lint clean |

Single phase — this is a direct port of proven mechanics into a new runtime.

## 5. Risks & Mitigations

| Risk                    | Mitigation                                         |
| ----------------------- | -------------------------------------------------- |
| Pattern drift vs Python | Memory `mem.artifact-pattern-sync` + SYNC comments |
| Unix socket unavailable | Fail-silent wrapper (DEC-094-02)                   |

## 6. Progress Tracking

- [ ] P01 complete
