---
id: IP-095
slug: requirement_frontmatter_populate_tags_ext_id_ext_url_in_creation_and_sync
name: 'Implementation Plan - Requirement frontmatter: populate tags, ext_id, ext_url in creation and sync'
created: '2026-03-14'
updated: '2026-03-14'
status: draft
kind: plan
aliases: []
---

```yaml supekku:plan.overview@v1
schema: supekku.plan.overview
version: 1
plan: IP-095
delta: DE-095
revision_links:
  aligns_with: []
specs:
  primary:
    - PROD-008
    - PROD-009
  collaborators: []
requirements:
  targets:
    - PROD-008.FR-001
    - PROD-009.FR-001
  dependencies: []
phases:
  - id: IP-095-P01
```

## 1. Summary

- **Delta**: DE-095 – Requirement frontmatter: populate tags, ext_id, ext_url in creation and sync
- **Specs Impacted**: PROD-008, PROD-009
- **Desired Outcome**: Requirements support `tags`, `ext_id`, `ext_url` end-to-end: CLI creation emits them, sync reads them from frontmatter.

## 2. Context & Constraints

- **Current Behaviour**: Creation omits these fields; sync ignores them on breakout files
- **Target Behaviour**: Full round-trip — create with fields → sync populates registry → list/show displays them
- **Dependencies**: None — the model and formatters already support these fields
- **Constraints**: Must not break inline `[tag]` extraction; all fields remain optional

## 3. Gate Check

- [x] Spec(s) referenced (PROD-008, PROD-009)
- [x] Test strategy identified (unit tests for creation and sync)
- [x] No workspace/config changes needed

## 4. Phase Overview

| Phase   | Objective               | Entrance Criteria | Exit Criteria               | Phase Sheet          |
| ------- | ----------------------- | ----------------- | --------------------------- | -------------------- |
| Phase 1 | Creation + Sync + Tests | Delta accepted    | All tests green, lint clean | `phases/phase-01.md` |

Single phase — the change is small, well-scoped, and touches only four files plus tests.

## 5. Testing & Verification Plan

- **New Cases**:
  - `create_requirement_breakout()` with tags/ext_id/ext_url → frontmatter contains them
  - Registry sync reads tags/ext_id/ext_url from breakout file frontmatter
  - Inline tags + frontmatter tags → sorted union in synced record
  - Existing tests remain green

## 6. Risks & Mitigations

| Risk                                      | Mitigation                                                               |
| ----------------------------------------- | ------------------------------------------------------------------------ |
| Inline tags and frontmatter tags conflict | Merge via sorted set union (matches `RequirementRecord.merge()` pattern) |

## 7. Progress Tracking

- [ ] Phase 1 complete
- [ ] Verification gates passed
