---
id: DE-100
slug: uniform_tag_conventions_match_tag_for_queries_tag_for_mutations
name: "Delta - Uniform tag conventions: --match-tag for queries, --tag for mutations"
created: "2026-03-17"
updated: "2026-03-17"
status: draft
kind: delta
aliases: []
relations:
  - type: follows
    target: DE-096
    summary: DE-096 landed --tag/--untag on all edit commands; this delta completes the query side
context_inputs:
  - type: delta
    id: DE-096
    summary: Landed --tag/--untag on all edit commands and frontmatter writer
applies_to:
  specs: [PROD-010]
  requirements: []
---

# DE-100 – Uniform tag conventions: --match-tag for queries, --tag for mutations

```yaml supekku:delta.relationships@v1
schema: supekku.delta.relationships
version: 1
delta: DE-100
revision_links:
  introduces: []
  supersedes: []
specs:
  primary:
    - PROD-010
  collaborators: []
requirements:
  implements: []
  updates: []
  verifies: []
phases: []
```

## 1. Summary & Context

- **Product Spec**: PROD-010 – CLI UX
- **Prior work**: DE-096 landed `--tag`/`--untag` on all `edit` commands
- **Design Revision**: [DR-100](./DR-100.md)
- **Implementation Plan**: [IP-100](./IP-100.md)

## 2. Motivation

Tag filtering on `list` commands is inconsistent:

- **3 commands** have `--tag` for filtering (ADRs, policies, standards)
- **1 command** has both `--tag` (metadata pre-filter) and `--match-tag` (scope match) — memories
- **12+ commands** have no tag filtering at all despite entities carrying `tags:` in frontmatter

Additionally, `--tag` is overloaded: it means "set a tag" in `edit` commands and "filter by tag" in `list` commands. This is confusing.

**Convention to establish:**
- `--tag`/`-t` = **mutator** (set/add a tag) — `edit` and `create` commands only
- `--match-tag` = **query/filter** (filter results by tag) — `list` commands only

## 3. Scope & Objectives

### Establish flag convention

| Flag | Semantics | Commands |
|------|-----------|----------|
| `--tag`/`-t` | **Mutator** — set/add a tag | `edit`, `create` |
| `--match-tag` | **Query** — filter by tag (OR, scope-style) | `list` |
| `--has-tag` | **Query** — metadata pre-filter (AND) | `list` (memories; available elsewhere if needed) |

### Rename existing `list` filters
- `list adrs --tag` → `--match-tag`
- `list policies --tag` → `--match-tag`
- `list standards --tag` → `--match-tag`
- `list memories --tag` (metadata AND pre-filter) → `--has-tag` (keeps distinct AND semantics)
- `list memories --match-tag` (scope OR match) → unchanged

### Add `--match-tag` to list commands missing it

All entity types have `tags:` in frontmatter (via base metadata). These list commands need `--match-tag`:

- `list backlog` (registry already has `tag` param — just needs CLI wiring)
- `list specs` (registry already has `tag` param — just needs CLI wiring)
- `list deltas`
- `list changes`
- `list requirements`
- `list revisions`
- `list audits`
- `list plans`
- `list cards`
- `list drift`
- `list issues` / `list problems` / `list improvements` / `list risks` (if separate from `list backlog`)

### Registry layer
- Where registries already have `tag=` filter param: wire to CLI
- Where registries lack it: add `tag` param to `filter()` method
- `list memories`: reconcile the two tag filter paths into one `--match-tag`

## 4. Out of Scope

- `--tag` on remaining `create` commands (DE-096 noted this as follow-up)
- Other array field filters (`--match-owner`, `--match-category`, etc.)

## 5. Risks & Mitigations

- **Risk**: Renaming `--tag` to `--match-tag` on ADRs/policies/standards is a breaking CLI change – _Likelihood_: medium – _Impact_: low (no known external consumers) – _Mitigation_: Clean rename, no deprecation shim needed at this stage
- **Risk**: `list memories` has two distinct tag semantics — resolved by splitting to `--has-tag` (AND metadata) and `--match-tag` (OR scope)

## 6. Verification Strategy

- Existing tests for `list adrs/policies/standards` tag filtering pass after rename
- New tests for `--match-tag` on previously unsupported list commands
- `just check` green
