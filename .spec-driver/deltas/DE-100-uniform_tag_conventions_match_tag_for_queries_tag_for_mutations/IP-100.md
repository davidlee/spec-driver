---
id: IP-100
slug: uniform_tag_filtering_across_list_commands
name: "Implementation Plan - Uniform --tag filtering across all list commands"
created: "2026-03-17"
updated: "2026-03-17"
status: draft
kind: plan
aliases: []
---

```yaml supekku:plan.overview@v1
schema: supekku.plan.overview
version: 1
plan: IP-100
delta: DE-100
revision_links:
  aligns_with:
    - DR-100
specs:
  primary:
    - PROD-010
  collaborators: []
requirements:
  targets: []
  dependencies: []
phases:
  - id: IP-100-P01
    name: Add --tag filtering to all list commands
    status: planned
  - id: IP-100.PHASE-01
```

## 1. Summary

- **Delta**: DE-100 – Uniform --tag filtering across all list commands
- **Specs Impacted**: PROD-010
- **Desired Outcome**: Every `list` command for frontmatter-bearing entities supports `--tag`/`-t` (repeatable, OR logic, ANDed with other filters).

## 2. Context & Constraints

- **Current Behaviour**: 3 list commands have `--tag` (single-value). 12+ do not.
- **Target Behaviour**: All list commands for taggable entities have `--tag` (repeatable, OR). `PlanSummary` model gains `tags` field.
- **Dependencies**: None — DE-096 already landed edit-side support.
- **Constraints**: No registry changes. All filtering inline in CLI layer.

## 3. Gate Check

- [x] DR-100 approved
- [x] No open design questions
- [ ] Test strategy: CLI integration tests for each command

## 4. Phase Overview

| Phase   | Objective                                                             | Entrance Criteria | Exit Criteria                                           | Phase Sheet          |
| ------- | --------------------------------------------------------------------- | ----------------- | ------------------------------------------------------- | -------------------- |
| Phase 1 | Add --tag to all list commands, make repeatable, add PlanSummary.tags | DR-100 approved   | All commands have --tag, tests pass, `just check` green | `phases/phase-01.md` |

Single phase — the work is mechanical and uniform across commands.

## 5. Testing & Verification Plan

- **Existing tests**: Update any tests for `list adrs/policies/standards --tag` to cover repeatable OR
- **New tests**: CLI tests for `--tag` on backlog, specs, deltas, changes, requirements, revisions, audits, plans
- **Key scenarios**: single tag, multiple tags (OR), no match, AND with --status, AND with --filter

## 6. Risks & Mitigations

| Risk                                                                                                      | Mitigation                                 | Owner |
| --------------------------------------------------------------------------------------------------------- | ------------------------------------------ | ----- |
| Making --tag repeatable changes type from `str \| None` to `list[str] \| None` on ADRs/policies/standards | OR with single value = identical behaviour | Dev   |

## 7. Progress Tracking

- [ ] Phase 1 complete
- [ ] `just check` green
