---
id: IP-110
slug: workflow_schema_export_for_cross_project_contract_testing
name: Implementation Plan - Complete schema show coverage for all registered block types
created: "2026-03-22"
updated: "2026-03-22"
status: draft
kind: plan
aliases: []
---

```yaml supekku:plan.overview@v1
schema: supekku.plan.overview
version: 1
plan: IP-110
delta: DE-110
revision_links:
  aligns_with:
    - DR-110
specs:
  primary:
    - PROD-012
  collaborators: []
requirements:
  targets: []
  dependencies: []
phases:
  - id: IP-110-P01
```

```yaml supekku:verification.coverage@v1
schema: supekku.verification.coverage
version: 1
subject: IP-110
entries:
  - artefact: VT-110-001
    kind: VT
    requirement: PROD-012.FR-001
    status: planned
    notes: "All 15 show schema --format=json-schema calls produce valid JSON Schema"
  - artefact: VT-110-002
    kind: VT
    requirement: PROD-012.FR-001
    status: planned
    notes: "All 15 show schema --format=yaml-example calls produce valid YAML"
  - artefact: VT-110-003
    kind: VT
    requirement: PROD-012.FR-001
    status: planned
    notes: "Existing 6 types produce output consistent with pre-change behaviour"
```

## 1. Summary

- **Delta**: DE-110 — Complete schema show coverage for all registered block types
- **Design Revision**: DR-110 (approved)
- **Desired Outcome**: Every registered block type supports `--format=json-schema` and `--format=yaml-example` via the existing `show schema` CLI command.

## 2. Context & Constraints

- **Current Behaviour**: 6 of 15 registered block schemas support json-schema/yaml-example output. 9 fail with "not yet available" due to a hardcoded lookup dict in `schema.py`.
- **Target Behaviour**: 15/15 working. Hardcoded dict eliminated.
- **Dependencies**: None blocking. DE-109 enums already present.
- **Constraints**: STD-002 lint compliance. POL-001 reuse. Single-session scope.

## 3. Gate Check

- [x] DR-110 approved
- [x] No blocking dependencies
- [x] Test strategy: unit tests for json-schema and yaml-example output per block type
- [x] Single phase — well-bounded refactor + authoring

## 4. Phase Overview

| Phase    | Objective                                                           | Entrance Criteria | Exit Criteria                                                                   | Phase Sheet          |
| -------- | ------------------------------------------------------------------- | ----------------- | ------------------------------------------------------------------------------- | -------------------- |
| Phase 01 | Wire metadata to BlockSchema, fill all gaps, replace hardcoded dict | DR-110 approved   | All 15 types produce valid json-schema and yaml-example; lint clean; tests pass | `phases/phase-01.md` |

## 5. Phase Detail Snapshot

- **Design Revision**: DR-110
- **Active Phase Sheet**: `phases/phase-01.md`

## 6. Testing & Verification Plan

- **Updated Suites**: `supekku/cli/schema_test.py`
- **New Cases**: Parameterised test iterating all 15 block types for json-schema and yaml-example
- **Regression**: Existing 6 types must produce structurally identical output

## 7. Risks & Mitigations

| Risk                                                  | Mitigation                                                     | Owner |
| ----------------------------------------------------- | -------------------------------------------------------------- | ----- |
| Workflow example authoring takes longer than expected | Base on real YAML files where available; keep examples minimal | Dev   |

## 8. Open Questions & Decisions

None — all resolved in DR-110.

## 9. Progress Tracking

- [ ] Phase 01 complete
- [ ] Verification gates passed
