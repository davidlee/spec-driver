---
id: DE-111
slug: spec_model_pydantic_conversion_and_schema_docs
name: Delta - Spec model Pydantic conversion and schema docs
created: "2026-03-22"
updated: "2026-03-22"
status: completed
kind: delta
aliases: []
relations:
  - type: follows_from
    target: DE-107
    description: Deferred items from DE-107 Pydantic migration
context_inputs:
  - type: delta
    id: DE-107
    note: Phase 2 deferred Spec model; Phase 3 deferred frontmatter metadata and schema docs
applies_to:
  specs: [SPEC-116]
  requirements: []
---

# DE-111 – Spec model Pydantic conversion and schema docs

```yaml supekku:delta.relationships@v1
schema: supekku.delta.relationships
version: 1
delta: DE-111
revision_links:
  introduces: []
  supersedes: []
specs:
  primary:
    - SPEC-116
  collaborators: []
requirements:
  implements: []
  updates: []
  verifies: []
phases: []
```

## 1. Summary

Follow-on from DE-107. Resolves three deferred items:

1. **Spec model** — conscious decision to keep as `@dataclass(frozen=True)` (DEC-111-001)
2. **plan.py metadata** — add missing `plan` and `delta` fields (DEC-111-002)
3. **Schema docs** — collapses into item 2 (auto-discovered)

## 2. Motivation

DE-107 converted 16 models but deferred the Spec model (structurally different) and plan.py metadata (DE-106 gap). This delta closes both with minimal scope.

## 3. Scope

- One code change: `plan.py` metadata fields
- One design decision: Spec stays as dataclass (documented, not an oversight)
- Schema docs update automatically

## 4. Out of Scope

- Kind-aware frontmatter validation beyond plan/phase
- Spec model restructuring (no benefit identified)

## 5. Verification

- Schema command shows plan/delta fields
- Existing tests pass
- Lint clean
