---
id: IP-113
slug: dead_code_purge_orphaned_scripts_hollow_functions_stale_migrations
name: "Implementation Plan - Dead code purge: orphaned scripts, hollow functions, stale migrations"
created: "2026-03-22"
updated: "2026-03-22"
status: accepted
kind: plan
aliases: []
---

```yaml supekku:plan.overview@v1
schema: supekku.plan.overview
version: 1
plan: IP-113
delta: DE-113
revision_links:
  aligns_with: []
specs:
  primary:
    - SPEC-110
    - SPEC-112
  collaborators: []
requirements:
  targets: []
  dependencies: []
phases:
  - id: IP-113-P01
```

```yaml supekku:verification.coverage@v1
schema: supekku.verification.coverage
version: 1
subject: IP-113
entries:
  - artefact: VT-existing
    kind: VT
    requirement: SPEC-110
    status: existing
    notes: Full test suite via `just check` — no new tests needed, existing coverage validates zero behaviour change.
```

## 1. Summary

- **Delta**: DE-113 — Dead code purge: orphaned scripts, hollow functions, stale migrations
- **Specs Impacted**: SPEC-110 (cli), SPEC-112 (scripts/cli)
- **Desired Outcome**: `supekku/scripts/` contains only scripts with live entry points or CLI imports. Dead code, hollow functions, stale migrations, and misnamed modules are removed or corrected.

## 2. Context & Constraints

- **Current Behaviour**: 15 orphaned scripts, 7 hollow `pass`-body display functions, a stale migration, a misnamed production module, and an unreachable duplicate doc generator.
- **Target Behaviour**: All orphaned scripts deleted. `complete_delta` logic relocated to `scripts/lib/changes/completion.py`. `registry_migration.py` renamed to `registry_v2.py`. STD-004 compliance achieved.
- **Dependencies**: None. First delta in quality remediation sequence.
- **Constraints**: Zero behaviour change. `just check` must pass.

## 3. Gate Check

- [x] DR-113 accepted
- [x] Test strategy identified (existing suite, no new tests)
- [x] No workspace/config changes needed (pyproject.toml entry points unaffected)

## 4. Phase Overview

| Phase                  | Objective                                                                                     | Entrance Criteria | Exit Criteria                                       | Phase Sheet          |
| ---------------------- | --------------------------------------------------------------------------------------------- | ----------------- | --------------------------------------------------- | -------------------- |
| P01 — Purge & relocate | Delete orphaned scripts, relocate complete_delta, rename registry_migration, clean stale TODO | DR-113 accepted   | `just check` passes, grep confirms no stale imports | `phases/phase-01.md` |

Single phase — the work is mechanical and interdependencies are minimal.

## 5. Testing & Verification Plan

- **Updated Suites**: None — deleted code removes tests, no new tests needed.
- **Verification**: `just check` (full suite + lint), grep for stale imports.
- **Rollback**: `git revert` — pure deletion is trivially reversible.

## 6. Risks & Mitigations

| Risk                                   | Mitigation                                                  | Owner |
| -------------------------------------- | ----------------------------------------------------------- | ----- |
| Orphaned script invoked externally     | Verified: pyproject.toml, Justfile, CI configs show no refs | —     |
| `complete_delta` relocation breaks CLI | Mechanical import update, existing test coverage            | —     |

## 7. Progress Tracking

- [ ] Phase 01 complete
- [ ] `just check` passes
- [ ] Delta closed
