---
id: IP-114
slug: god_file_structural_splits
name: "Implementation Plan - God-file structural splits"
created: "2026-03-22"
updated: "2026-03-22"
status: accepted
kind: plan
aliases: []
---

```yaml supekku:plan.overview@v1
schema: supekku.plan.overview
version: 1
plan: IP-114
delta: DE-114
revision_links:
  aligns_with: []
specs:
  primary:
    - SPEC-110
    - SPEC-114
    - SPEC-122
  collaborators: []
requirements:
  targets: []
  dependencies: []
phases:
  - id: IP-114-P01
  - id: IP-114-P02
  - id: IP-114-P03
```

```yaml supekku:verification.coverage@v1
schema: supekku.verification.coverage
version: 1
subject: IP-114
entries:
  - artefact: VT-existing
    kind: VT
    requirement: SPEC-110
    status: existing
    notes: Full test suite validates zero behaviour change. No new tests needed except import path updates.
```

## 1. Summary

- **Delta**: DE-114 — God-file structural splits
- **Specs Impacted**: SPEC-110 (cli), SPEC-114 (blocks/metadata), SPEC-122 (requirements)
- **Desired Outcome**: 5 god files (8,371 lines) split into ~30 focused modules, all ≤500 lines (one justified exception at ~540).

## 2. Context & Constraints

- **Current Behaviour**: Five files exceed 1,000 lines with low cohesion.
- **Target Behaviour**: Each file has one cohesive responsibility ≤500 lines. Existing import paths preserved via re-exports.
- **Dependencies**: DE-113 (completed). DE-116 (upcoming — registry split must not obstruct protocol work).
- **Constraints**: Zero behaviour change. `RequirementsRegistry` public surface stays on the class (DE-116 alignment).

## 3. Gate Check

- [x] DR-114 accepted (3 adversarial passes, zero blockers)
- [x] Test strategy: existing suite, import path updates only
- [x] No workspace/config changes needed

## 4. Phase Overview

| Phase                                   | Objective                                                         | Entrance        | Exit                                                  | Sheet                |
| --------------------------------------- | ----------------------------------------------------------------- | --------------- | ----------------------------------------------------- | -------------------- |
| P01 — CLI layer                         | Split `common.py` then `list.py`                                  | DR-114 accepted | Both files split, lint+tests pass, no file >500 lines | `phases/phase-01.md` |
| P02 — Lib: creation + workflow_metadata | Split `creation.py` and `workflow_metadata.py`                    | P01 complete    | Both files split, lint+tests pass                     | `phases/phase-02.md` |
| P03 — Lib: requirements registry        | Split `requirements/registry.py` (standalone function extraction) | P02 complete    | Registry split, lint+tests pass, import graph acyclic | `phases/phase-03.md` |

**Rationale**: `list.py` imports from `common.py` — split common first. `creation.py` and `workflow_metadata.py` are independent. `requirements/registry.py` is hardest (method→function extraction) — do last.

## 5. Testing & Verification

- **Method**: `uv run ruff check`, `uv run python -m pytest supekku` after each split
- **Acceptance**: All existing tests pass. `wc -l` confirms size targets. `grep` confirms no stale imports.
- **Rollback**: `git revert` — each phase is a self-contained commit series.

## 6. Risks & Mitigations

| Risk                                                    | Mitigation                                                                          |
| ------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| Circular imports after `list/` package conversion       | Standard Typer pattern; `__init__.py` defines `app` before importing sub-modules    |
| `RequirementsRegistry` extraction breaks sync semantics | Full call graph mapped in DR-114; mutation-in-place preserved; 2,787 lines of tests |
| Re-export shims calcify                                 | Follow-up backlog issue tracked in DR-114 §9                                        |

## 7. Progress Tracking

- [x] Phase 01 complete (CLI layer)
- [x] Phase 02 complete (creation + workflow_metadata)
- [x] Phase 03 complete (requirements registry)
- [ ] Delta closed
