---
id: IP-115
slug: core_utility_extraction_parse_date_sequential_ids_title_extraction_atomic_write_block_patterns
name: "Implementation Plan - Core utility extraction: parse_date, sequential IDs, title extraction, atomic write, block patterns"
created: "2026-03-22"
updated: "2026-04-10"
status: draft
kind: plan
aliases: []
---

```yaml supekku:plan.overview@v1
schema: supekku.plan.overview
version: 1
plan: IP-115
delta: DE-115
revision_links:
  aligns_with: [DR-115]
specs:
  primary:
    - SPEC-117
    - SPEC-126
    - SPEC-127
  collaborators: []
requirements:
  targets: []
  dependencies: []
phases:
  - id: IP-115-P01
  - id: IP-115-P02
```

```yaml supekku:verification.coverage@v1
schema: supekku.verification.coverage
version: 1
subject: IP-115
entries:
  - artefact: VA-115-grep
    kind: VA
    requirement: DE-115
    status: planned
    notes: grep acceptance checks — parse_date, next_sequential_id, _matches_pattern, create_title_slug
  - artefact: VT-115-unit
    kind: VT
    requirement: DE-115
    status: planned
    notes: unit tests for each extracted utility in core/ and blocks/yaml_utils.py
  - artefact: VH-115-check
    kind: VH
    requirement: DE-115
    status: planned
    notes: just check passes clean after all phases
```

## 1. Summary

- **Delta**: DE-115 — Core utility extraction
- **Specs Impacted**: SPEC-117, SPEC-126, SPEC-127
- **Design Revision**: DR-115
- **Desired Outcome**: All duplicated utility logic consolidated into canonical `core/` and `blocks/yaml_utils.py` homes; all callers updated; no orphaned originals; `just check` passes.

## 2. Context & Constraints

- **Current Behaviour**: 8 sequential ID generators, 3 `parse_date` copies, 3 `create_title_slug` wrappers, 6 block-pattern definitions, 2 `_matches_pattern` copies, 3 atomic-write implementations (1 named + 2 inlined) — spread across domain modules.
- **Target Behaviour**: One canonical implementation per utility; all duplicates deleted or delegating.
- **Dependencies**: DE-114 (should be complete — smaller files to work in)
- **Constraints**: Pure extract-and-delegate only. No behaviour changes. `just check` gate after each phase.

## 3. Gate Check

- [x] Spec(s) identified: SPEC-117, SPEC-126, SPEC-127
- [x] DR-115 written and consistent with DE-115 scope
- [x] Test strategy: unit tests per extraction, grep acceptance criteria at close
- [ ] Confirm DE-114 is complete before starting P01

## 4. Phase Overview

| Phase | Objective | Entrance Criteria | Exit Criteria | Phase Sheet |
| ----- | --------- | ----------------- | ------------- | ----------- |
| P01 — Utility extractions | Extract `parse_date`, delete `create_title_slug`, extract `matches_pattern`, promote `atomic_write`, add `make_block_pattern`, add `extract_h1_title` | DE-114 complete; DR-115 approved | `just check` passes; grep confirms 1 `parse_date`, 0 `create_title_slug`, 0 `_matches_pattern` private copies, 0 inline block-pattern `re.compile` defs | `phases/phase-01.md` |
| P02 — Sequential ID consolidation | Write `next_sequential_id` in `core/ids.py`; update all 8 call sites | P01 complete | `just check` passes; grep returns exactly 1 `next_sequential_id` definition; all original generator functions deleted or delegating | `phases/phase-02.md` |

## 5. Phase Detail Snapshot

- **Research Notes**: `DE-115/notes.md`
- **Design Revision**: `DE-115/DR-115.md`
- **Active Phase Sheet**: `phases/phase-01.md`
- **Parallelisable Work**: P01 extractions are independent within the phase (can be committed separately)

## 6. Testing & Verification Plan

- Unit tests for each new function in its module (`core/dates_test.py`, `core/ids_test.py`, `core/io_test.py`, extension of `core/spec_utils_test.py`, extension of `blocks/yaml_utils_test.py`)
- Extension of `core/filters_test.py` for `matches_pattern`
- Existing tests must remain green throughout (regression guard)
- Grep acceptance criteria (see DR-115 §5) run as VA at end of each phase

## 7. Risks & Mitigations

| Risk | Mitigation | Owner |
| ---- | ---------- | ----- |
| `backlog/registry.py:next_identifier` or `CardRegistry.next_id` uses a materially different algorithm | Resolved: all use same core algorithm; consolidated via `next_sequential_id` with `separator` param | resolved |
| `state_io.py`/`handoff_io.py` inline atomic-write differently from `review_io._atomic_write` | Resolved: identical pattern confirmed | resolved |
| Subtle behavioural difference between "identical" copies | Diff each pair before deleting; existing tests catch regressions | implementer |

## 8. Open Questions & Decisions

- [ ] Confirm DE-114 is complete before P01 begins
- [x] ~~Confirm `backlog/registry.py:next_identifier` algorithm~~ — Resolved: same core algorithm, consolidated via `next_sequential_id`
- [x] ~~Confirm `state_io.py`/`handoff_io.py` atomic-write situation~~ — Resolved: both inline identical fd/mkstemp/replace pattern

## 9. Progress Tracking

- [ ] P01 complete — utility extractions
- [ ] P02 complete — sequential ID consolidation
- [ ] All grep acceptance criteria verified
- [ ] `just check` clean

## 10. Notes / Links

- DE-114 (dependency — file splits): must be complete before P01
- DE-116 (downstream — registry consolidation): will use the shared utilities from this delta
