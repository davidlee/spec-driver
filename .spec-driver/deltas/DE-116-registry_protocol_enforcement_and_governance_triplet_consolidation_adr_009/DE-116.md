---
id: DE-116
slug: registry_protocol_enforcement_and_governance_triplet_consolidation_adr_009
name: Delta - Registry protocol enforcement and governance triplet consolidation (ADR-009)
created: "2026-03-22"
updated: "2026-06-02"
status: in-progress
kind: delta
---

# DE-116 – Registry protocol enforcement and governance triplet consolidation

```yaml supekku:delta.relationships@v1
schema: supekku.delta.relationships
version: 1
delta: DE-116
revision_links:
  introduces: []
  supersedes: []
specs:
  primary:
    - SPEC-117
    - SPEC-126
    - SPEC-127
  collaborators: []
requirements:
  implements: []
  updates: []
  verifies: []
phases: []
```

## 1. Summary & Context

- **Technical Spec(s)**: SPEC-117 (decisions), SPEC-126 (policies), SPEC-127 (standards)
- **Change Drivers**: ADR-009 (standard registry API convention) mandates a canonical surface but enforcement is purely documentary. The Decision/Policy/Standard registries are structural triplets with 80%+ code duplication.
- **Governance**: ADR-009, POL-001

## 2. Motivation

ADR-009 established the canonical registry API (`find/collect/iter/filter`) but deferred Protocol/ABC standardisation. The result: 10 registries independently implement the surface with drift in keyword conventions, method availability, and semantics. The three governance registries (Decision, Policy, Standard) are the worst case — `parse_date()`, `collect()`, `_parse_X_file()`, `write()`, `iter()`, `find()`, `to_dict()`, and creation flows are near-identical, differing only in prefix strings and glob patterns.

Specific drift:

- `iter(status=...)` is keyword-only in some registries, positional in others
- `DriftLedgerRegistry` omits `filter()` entirely
- `CardRegistry` uses `lane` not `status`
- `RequirementsRegistry.sync()` has completely different semantics from `DecisionRegistry.sync()`
- Constructor conventions vary: keyword-only `root` vs positional vs required

> **Scope revised 2026-06-01** (see [DR-116](./DR-116.md)). The original card predated the
> `spec_driver` layered-migration program (DE-125/127/128). Building the base in legacy `core/`
> with registry-owned backlinks would replant the misplacement that program is undoing. Per user
> direction ("do it properly"), DE-116 now also **migrates the records + registries into
> `spec_driver.domain`** and **hoists backlink composition to `Workspace`**. The original §2
> drift analysis is retained as historical motivation.

## 3. Scope & Objectives

- **Primary Outcomes**:
  - Define `RegistryProtocol[T]` (ADR-009 read surface: `find/collect/iter/filter`) in
    `spec_driver/domain/registries/frontmatter.py` — typed + `runtime_checkable`.
  - Extract `FrontmatterFileRegistry[T]` ABC (template-method `_parse_file` + abstract
    `_build_record`/`_artifact_dir` + overridable `_resolve_status`); parameterised by prefix,
    YAML root key, record type, backlink declaration.
  - Collapse Decision/Policy/Standard registries onto the base (~330L → ~40L each); delete
    duplicated bodies and redundant `load()` classmethods.
  - Make registries **pure**: remove `_build_backlinks`; **hoist backlink composition to
    `Workspace`** (`build_backlinks_multi`), driven by each registry's declarative
    `backlink_inputs`. Registries import nothing from `domain.relations`.
  - `RegistryProtocol[T]` covers **`find`/`collect`/`iter` only**; `filter` is excluded from the
    typed generic protocol (keyword-only per-registry signatures are not structural subtypes) — ADR-009 §5.
  - **Migrate** records → `spec_driver/domain/records/`, registries → `spec_driver/domain/
    registries/` (importing `spec_driver.core.*`, **not** legacy supekku core); leave re-export shims
    at legacy paths. **Legacy `supekku` consumers stay unedited; the ≤3 `spec_driver`-internal
    consumers migrate to canonical paths now.**
  - Unify creation **impl-only** (P3): one shared private engine; public `create_X` / `*Options` /
    `*Result` / `*AlreadyExistsError` and exact CLI messages preserved as thin wrappers.
  - Keep `iter()` **positional-or-keyword** per ADR-009 §1 (corrects the original "keyword-only").
- **Operational Constraints**: External CLI stdout + public creation symbols unchanged. Generated
  registry YAML byte-identical (backlink-hoist safety net). `domain.registries` must not import
  `domain.relations`; new domain modules must not import `supekku.scripts.lib.core` (layer contract).
- **Sequencing**: registry core + migration + backlink hoist (R) land first (P1–P2, committed green);
  creation unification is an isolated gated phase P3 so a creation regression cannot block R.
- **Dependencies**: DE-115 (helpers — done); DE-125/127/128 (migration program — done).

## 4. Out of Scope

- Updating the **legacy `supekku` consumers** to canonical paths + deleting shims (tracked follow-up
  delta). The ≤3 `spec_driver`-internal consumers ARE migrated in this delta (see §3).
- `DriftLedgerRegistry.filter()` addition (separate registry; deferred to a follow-up).
- `RequirementsRegistry` / `CardRegistry` (different domains; separate deltas).
- Migrating `creation` / `Workspace` into `spec_driver.orchestration` (kept legacy this delta).
- Persistence **naming** unification (`sync`/`write`/`save`) — ADR-009 §5 defers this.

## 5. Approach Overview

- **P0 (spike)**: Prove `RegistryProtocol` (find/collect/iter) conformance under `ty` + `isinstance`
  before migrating. `filter` is excluded from the typed protocol (asserted by test).
- **P1**: Records → `domain/records/`; `RegistryProtocol` + `FrontmatterFileRegistry` →
  `domain/registries/frontmatter.py` (importing `spec_driver.core.*`); `DecisionRegistry` onto base;
  legacy shims; migrate the ≤3 `spec_driver`-internal consumers to canonical paths; green.
- **P2**: `PolicyRegistry` + `StandardRegistry` onto base; hoist backlinks to `Workspace`;
  golden-YAML fixture diff; shim-compat suite; import-linter/ty green. **R landable/committed here.**
- **P3**: Creation impl-only unification (shared private engine; public wrappers + messages
  preserved). `cli/create.py` unchanged. Isolated gate.
- **P4**: Verification sweep — `just check`, `uvx import-linter`, `uv run ty check`, core-import +
  internal-consumer gates; file shim-debt follow-up.

## 6. Verification Strategy

- **Acceptance Criteria**:
  - `just check` + `uvx import-linter` + `uv run ty check` all green.
  - `spec_driver/domain/registries/**` imports nothing from `domain.relations` (grep = 0).
  - `spec_driver/domain/{records,registries}/**` imports nothing from `supekku.scripts.lib.core`
    (grep = 0) — canonical `spec_driver.core.*` only.
  - No `spec_driver/**` (non-shim) imports the 6 symbols from `supekku.*` (internal-consumer gate).
  - Decision/Policy/Standard registries share one base; **zero duplicated method bodies** — the 3
    `domain/registries/{decision,policy,standard}.py` define only `_build_record`, `_artifact_dir`,
    `filter`, class attrs (+ ADR symlink methods); none redefine `collect/_parse_file/iter/find/
    write/sync` (grep = 0). No `load()` classmethods remain.
  - Golden registry-YAML diff empty before/after, run over a **fixture corpus with known
    cross-references** (the live corpus has zero backlinks — a live diff is vacuous; see DR AR-2).
  - `RegistryProtocol` (find/collect/iter) conformance verified under `ty`; runtime `isinstance`
    confirms method presence.
  - **Shim compatibility suite** on legacy paths green (imports/isinstance/patch/`__all__`); legacy
    `supekku` consumers pass unedited.
  - Public creation surface unchanged: `create_X` / `*Options` / `*Result` / `*AlreadyExistsError`
    importable; CLI error messages byte-identical (`"Policy file already exists"` etc).

## 7. Follow-ups & Tracking

- **Depends on**: DE-115, DE-125/127/128 (all complete).
- **Dependency of**: DE-120 (Workspace facade benefits from consistent registry API).
- **Follow-ups created by this delta**:
  - Consumer migration + shim removal (rewrite the 37 legacy-path imports → canonical).
  - `DriftLedgerRegistry.filter()` addition (registry-surface completeness).
- **Sequence**: Sprint 4 of quality remediation; first registry migration into `spec_driver.domain`.

```yaml supekku:delta.context_inputs@v1
schema: supekku.delta.context_inputs
version: 1
entries: []
```

```yaml supekku:delta.risk_register@v1
schema: supekku.delta.risk_register
version: 1
risks:
  - id: R1
    risk: Backlink hoist to Workspace changes generated YAML (ordering, clearing, edge cases).
    likelihood: medium
    impact: high
    mitigation: Golden byte-identical YAML diff before/after as a gating VT; revert Workspace change on any diff.
  - id: R2
    risk: filter()/iter() signature variance fails strict RegistryProtocol conformance under ty.
    likelihood: medium
    impact: medium
    mitigation: P0 spike before migration; documented fallback (find/collect/iter strict, filter existence-only).
  - id: R3
    risk: Re-export shims drift — new code added to legacy path instead of canonical location.
    likelihood: medium
    impact: medium
    mitigation: Shims carry canonical-location docstring; shim-debt follow-up tracked; consumers untouched this delta.
  - id: R4
    risk: record_artifact() added to Standard creation is an unintended behaviour change (event recording).
    likelihood: low
    impact: low
    mitigation: Flagged open question; confirm acceptable or preserve current STD behaviour.
  - id: R5
    risk: Migration blast radius (37 consumers, first registry migration) destabilises the suite.
    likelihood: low
    impact: high
    mitigation: Incremental recipe (records then registries), import-linter + full suite after each step.
```
