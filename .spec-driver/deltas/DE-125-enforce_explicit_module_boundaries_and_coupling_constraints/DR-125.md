---
id: DR-125
slug: enforce_explicit_module_boundaries_and_coupling_constraints
name: Design Revision - Enforce explicit module boundaries and coupling constraints
created: "2026-03-23"
updated: "2026-03-23"
status: draft
kind: design_revision
aliases: []
owners: []
relations:
  - type: implements
    target: DE-125
delta_ref: DE-125
source_context:
  - type: document
    id: supekku/scripts/lib/__init__.py
    note: "Symptom: God-init loading 77+ modules"
  - type: delta
    id: DE-124
    note: "Blocked by entangled presentation/domain logic"
code_impacts:
  - path: supekku/scripts/lib/
    action: delete
    description: "Entire directory re-homed into layer-aligned structure"
  - path: spec_driver/
    action: create
    description: "New top-level package housing the explicit layers"
  - path: pyproject.toml
    action: modify
    description: "Add import-linter dependency and contract definitions"
verification_alignment:
  - id: VA-125-001
    impact: new
    notes: "import-linter contract enforcing downward-only dependencies"
design_decisions:
  - id: DEC-125-001
    summary: "Adopt strict 5-layer architecture (Core → Models → Domain → Orchestration → Presentation)"
  - id: DEC-125-002
    summary: "Re-home supekku/scripts/lib into spec_driver/* to make architecture explicit in the file tree"
  - id: DEC-125-003
    summary: "Enforce coupling rules automatically via import-linter in CI"
  - id: DEC-125-004
    summary: "Models layer is pure data; no I/O, no registry imports"
open_questions:
  - id: OQ-125-001
    text: "Should the CLI and TUI move into `spec_driver/presentation/` or remain as `spec_driver/cli/` and `spec_driver/tui/` alongside the layers?"
  - id: OQ-125-002
    text: "Does `autobahn` remain a separate top-level package in the monorepo, or move inside the `spec_driver` namespace?"
---

# DR-125 – Enforce explicit module boundaries and coupling constraints

## 1. Executive Summary

- **Delta**: [DE-125](./DE-125.md)
- **Status**: draft
- **Last Updated**: 2026-03-23
- **Synopsis**: Eradicate the `supekku/scripts/lib` dumping ground. Establish a strict, 5-layer architecture under `spec_driver/` with downward-only coupling constraints enforced by CI, governed by **POL-003**.

## 2. Problem & Constraints

- **Current Behaviour**: `spec-driver` has a flat, obfuscatory file hierarchy (`supekku/scripts/lib/*`) with an untamed dependency graph. Modules freely import from anywhere.
- **Drivers / Inputs**: 
  - The "god-init" problem (importing `lib` loads 77+ modules).
  - The difficulty of designing the Public API Facade (DE-124) without dragging in CLI concerns.
  - The risk of 10+ upcoming refactoring deltas executing without a governing structural blueprint, treating symptoms instead of the root cause.
- **Constraints / Guardrails**:
  - Do not change external CLI behaviour.
  - Do not change workflow artifact schemas.
  - The resulting structure must be mechanically verifiable by a linter.
- **Governing Policy**: **POL-003: explicit module boundaries and coupling constraints**.

## 3. Architecture Intent

We are shifting from an implicit, flat module graph to an explicit, layered architecture.

**The Golden Rule of Coupling (POL-003):**
> A layer may only import from its own layer, or layers strictly beneath it. Upward dependencies are forbidden. Sibling packages within a layer should minimize cross-coupling where possible.

### Target Architectural Layers (Bottom to Top)

#### 1. Core / Infrastructure Layer
*   **Target Path:** `spec_driver/core/`
*   **Contents:** `file_ops.py`, `workspace.py`, `blocks/` (raw YAML schema blocks), git helpers, base utilities.
*   **Responsibility:** Agnostic file system operations, YAML parsing, foundational pure functions.
*   **Coupling Rules:** Knows absolutely nothing about specs, deltas, or workflows. May only import Python standard library and approved third-party deps.

#### 2. Models Layer
*   **Target Path:** `spec_driver/models/`
*   **Contents:** The Pydantic models from DE-107 through DE-112.
*   **Responsibility:** Pure typed data structures, serialization, and schema validation.
*   **Coupling Rules:** May import from `core/`. Must **never** import registries, I/O functions, or orchestration logic. They are dumb, typed data containers.

#### 3. Domain / Registry Layer
*   **Target Path:** `spec_driver/domain/`
*   **Contents:** `registry_v2.py`, `specs/`, `backlog/`, `memory/`, `requirements/` resolution and graph logic.
*   **Responsibility:** Reading models from disk, maintaining the in-memory entity graph, resolving cross-artifact relations, tracking drift logic.
*   **Coupling Rules:** May import from `models/` and `core/`. Must **never** import orchestration/workflow logic or presentation concerns.

#### 4. Orchestration / Workflow Layer
*   **Target Path:** `spec_driver/workflow/` (and potentially `spec_driver/sync/`, `spec_driver/validation/`)
*   **Contents:** State machines, staleness evaluation, sync routines, phase sheet generation, cross-artifact validation passes.
*   **Responsibility:** The "verbs" of the system. Coordinating multi-step state transitions, executing syncs across the registry, evaluating operational readiness.
*   **Coupling Rules:** May import from all layers below. Must **not** import from the presentation layer. This layer represents the boundary for the Public API Facade (DE-124).

#### 5. Presentation Layer
*   **Target Path:** `spec_driver/cli/`, `spec_driver/tui/`
*   **Contents:** Typer CLI commands, Textual TUI applications.
*   **Responsibility:** Argument parsing, user interaction, formatting terminal output, rendering UIs.
*   **Coupling Rules:** May import from any layer below. Must **never** contain inline business logic or state transitions (enforces the Skinny CLI pattern).

## 4. Re-homing & Renaming Strategy

The `supekku` package name and `scripts/lib` path prefix are legacy cruft. The new package root will be `spec_driver/`.

**Mapping Examples:**
*   `supekku/scripts/lib/core/` → `spec_driver/core/`
*   `supekku/scripts/lib/file_ops.py` → `spec_driver/core/file_ops.py`
*   `supekku/scripts/lib/specs/models.py` → `spec_driver/models/specs.py`
*   `supekku/scripts/lib/registry_v2.py` → `spec_driver/domain/registry.py`
*   `supekku/scripts/lib/workflow/state_machine.py` → `spec_driver/workflow/state_machine.py`
*   `supekku/cli/` → `spec_driver/cli/`

## 5. Automated Enforcement (import-linter)

To ensure this architecture does not decay, we will introduce `import-linter` to the project.

**Proposed `pyproject.toml` configuration:**
```toml
[tool.importlinter]
root_package = "spec_driver"

[[tool.importlinter.contracts]]
name = "Architectural Layers"
type = "layers"
layers = [
    "spec_driver.cli | spec_driver.tui",
    "spec_driver.workflow",
    "spec_driver.domain",
    "spec_driver.models",
    "spec_driver.core",
]
```
This contract explicitly bans upward imports, causing the CI pipeline to fail if an orchestration module accidentally imports a CLI formatter, or if a model imports a registry.

## 6. Rollout & Sequence

1. **Blueprint & Policy**: Finalize and ratify **DR-125** and **POL-003**.
2. **Setup Enforcer**: Add `import-linter` to `pyproject.toml` and configure the initial contracts. Tune the linter and `pylint` for aggressive failure for the new `spec_driver/` package (while maintaining the existing threshold for `supekku/`).
3. **Establish API Shell**: Create the `spec_driver/__init__.py` and `spec_driver/workflow/__init__.py` skeleton. Temporarily re-import the necessary functions/types from `supekku/*` to fulfill the Public API Facade (DE-124) without moving the underlying code yet.
4. **Establish Internal Boundaries**: Create internal boundary APIs within `spec_driver/` (e.g. `spec_driver.domain`) that strictly re-export a reduced set of functions and types.
5. **The Strangler Fig Move**: Move individual files from `supekku/scripts/lib/` to `spec_driver/` one at a time. As each file is moved, fix its import graph so it strictly relies on the new `spec_driver` boundary APIs. The aggressive linter rules will validate each moved file.
6. **Decouple & Cleanup**: Eradicate `supekku/scripts/lib` entirely once the strangler fig migration is complete. This will naturally resolve the god-init problem (DE-114).

## 7. Open Questions

- **OQ-125-001**: Should `autobahn` remain a separate top-level Python package alongside `spec_driver`, or be pulled inside the `spec_driver` namespace (e.g. `spec_driver.autobahn`)?
- **OQ-125-002**: Are there specific modules in `supekku/scripts/lib` whose layer assignment is ambiguous (e.g., formatters)?
  _Proposed Answer: Formatters belong in the Presentation layer, or a dedicated `spec_driver/presentation/formatters/` package if shared between CLI and TUI._
