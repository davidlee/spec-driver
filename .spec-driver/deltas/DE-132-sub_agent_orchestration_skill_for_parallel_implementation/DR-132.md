---
id: DR-132
slug: sub_agent_orchestration_skill_for_parallel_implementation
name: Design Revision - Sub-agent orchestration skill for parallel implementation
created: "2026-04-16"
updated: "2026-04-16"
status: draft
kind: design_revision
aliases: []
owners: []
relations:
  - type: implements
    target: DE-132
delta_ref: DE-132
source_context:
  - type: reference
    ref: "https://github.com/jkaloger/lazyspec/blob/main/skills/build/SKILL.md"
    summary: "LazySpec build skill — sequential dispatch, two-stage review"
code_impacts:
  - path: ".spec-driver/skills/dispatch/SKILL.md"
    change: new
    summary: "Orchestrator skill — reads IP, batches tasks, dispatches sub-agents"
  - path: ".spec-driver/skills/dispatch-worker/SKILL.md"
    change: new
    summary: "Tourist-mode skill for sub-agents — lightweight spec-driver fluency"
  - path: ".spec-driver/skills.allowlist"
    change: modify
    summary: "Add dispatch and dispatch-worker to allowlist"
verification_alignment:
  - id: VH-001
    impact: new
    summary: "Manual: parallel dispatch on multi-phase IP"
  - id: VH-002
    impact: new
    summary: "Manual: worktree isolation verified"
  - id: VA-001
    impact: new
    summary: "Agent review: skill format compliance"
design_decisions:
  - id: DEC-132-01
    summary: "Batched dispatch: group tasks into ~120k token budgets, not one agent per task"
  - id: DEC-132-02
    summary: "Worktree isolation via Agent tool's isolation: worktree parameter"
  - id: DEC-132-03
    summary: "Peer to /execute-phase, not a wrapper"
  - id: DEC-132-04
    summary: "Sub-agents use inline instructions, not full /execute-phase ceremony"
  - id: DEC-132-05
    summary: "Batch review at phase boundary with tighter-pass escape hatch"
  - id: DEC-132-06
    summary: "Tourist-mode dispatch-worker skill for sub-agent spec-driver fluency"
  - id: DEC-132-07
    summary: "Sub-agents non-interactive; blockers reported in return summary"
  - id: DEC-132-08
    summary: "Auto-merge worktree branches on green checks"
open_questions:
  - text: "Sub-agent merge strategy — manual vs orchestrator-driven"
    owner: "future delta"
---

# DR-132 – Sub-agent orchestration skill for parallel implementation

## 1. Executive Summary

- **Delta**: [DE-132](./DE-132.md)
- **Status**: draft
- **Last Updated**: 2026-04-16
- **Synopsis**: New `/dispatch` skill that decomposes an IP into parallelizable
  batches, routes them to appropriate models, and executes via sub-agents in
  isolated worktrees.

## 2. Problem & Constraints

- **Current Behaviour**: `/implement` and `/execute-phase` run in a single
  agent context. Multi-phase IPs execute sequentially, wasting time and context
  on independent work. All tasks use the same model regardless of complexity.
- **Drivers**: User request; lazyspec build skill as prior art.
- **Constraints**:
  - Pure skill file (SKILL.md) — no Python code in this delta
  - Claude-only install target
  - Must not modify existing skills
  - Must degrade gracefully to sequential execution when parallelism isn't safe
- **Out of Scope**: Auto-merge of worktree branches, CI integration, non-Claude
  agent targets.

## 3. Architecture Intent

### 3.1 Orchestration Model

The `/dispatch` skill is an **orchestrator** — it reads structured artefacts
(IP, phase sheets, DR) and produces a dispatch plan, then executes it.

```
User invokes /dispatch <delta-id>
  │
  ├─ 1. Read IP, DR, phase sheets
  ├─ 2. Analyse: parallelism graph, model requirements, token budgets
  ├─ 3. Produce dispatch plan (batches, ordering, models, isolation)
  ├─ 4. Present plan to user for approval
  ├─ 5. Execute batches (parallel where safe, sequential where required)
  ├─ 6. Collect results + AC evidence summaries
  ├─ 7. Review at phase boundary (or tighter if warranted)
  └─ 8. Report outcomes, update notes
```

### 3.2 Batching Strategy

**Problem**: Per-task agent dispatch is context-wasteful (boot overhead, lost
shared understanding between related tasks). Per-phase dispatch wastes capacity
when phases are small.

**Solution**: Token-budget batching.

1. Estimate each task's token cost (heuristic: complexity tag from phase sheet,
   file count, description length).
2. Pack tasks into batches targeting ~120k tokens, respecting:
   - Task ordering constraints within a phase
   - File-scope overlap (tasks touching same files must be in same batch or sequential)
   - Model requirements (if any task in batch needs opus, batch gets opus)
3. Maximum batch size = one full phase.

### 3.3 Parallelism Analysis

Tasks/batches can run in parallel when:
- No data dependencies between them (no task references another's output)
- No file-scope overlap (disjoint set of touched files/modules)
- Different phases are independent (phase ordering in IP is respected)

The orchestrator builds a simple dependency graph from:
- Explicit `depends_on` in phase sheet tasks
- File-scope overlap detection (from task descriptions / `touches` fields)
- Phase ordering (phases execute in IP order unless marked independent)

### 3.4 Model Routing

Per-batch model assignment:

| Signal | Model |
|--------|-------|
| Task marked `complexity: high` or `requires: opus` | opus |
| Task involves architecture, design decisions, cross-cutting concerns | opus |
| Task is mechanical (rename, format, boilerplate, test scaffolding) | sonnet |
| Default (no signal) | sonnet |

Orchestrator assigns based on the hardest task in the batch.

### 3.5 Worktree Isolation

Parallel batches use `Agent` tool with `isolation: "worktree"`. This:
- Creates a temporary git worktree per agent
- Gives each agent a full, isolated copy of the repo
- Automatically cleans up worktrees with no changes
- Returns worktree path and branch name for changed worktrees

Sequential batches (or single-batch phases) run in the main worktree without
isolation overhead.

### 3.6 Sub-Agent Context Problem

This is the hardest design problem in the skill. Sub-agents need enough
project context to work correctly, but full ceremony would consume most of
their token budget.

#### 3.6.1 Sources of project-specific truth

| Source | Location | Nature |
|--------|----------|--------|
| Code standards, lint/test/commit policy | `CLAUDE.md` | Static, pre-known |
| Rules (auto-loaded by Claude Code) | `.claude/rules/*` | Static; available in worktrees since in-repo |
| Project conventions, commit policy | `doctrine.md` | Static, pre-known |
| Governance (ADRs, policies, standards) | `.spec-driver/decisions/`, etc. | Mostly static; orchestrator selects relevant subset |
| Memories scoped to touched files | `.spec-driver/memory/` | Reactive — depends on which files agent discovers during work |
| Design intent | DR, IP, phase sheet | Pre-known; orchestrator extracts |

#### 3.6.2 What the orchestrator pre-assembles

The orchestrator reads and condenses into the sub-agent prompt:

1. **Task scope**: Exact tasks from the phase sheet
2. **Design context**: Relevant DR excerpts, spec requirements
3. **File scope**: Which files/modules to touch (and which to avoid)
4. **Project policy digest**: Extracted from CLAUDE.md + doctrine.md —
   lint commands, test commands, commit conventions, code standards
5. **Governance excerpts**: Any ADRs/policies/standards relevant to the
   task scope (orchestrator reads them, includes pertinent sections)
6. **Pre-fetched memories**: `spec-driver list memories -p <path>` for
   each expected file scope, included verbatim

#### 3.6.3 What the sub-agent must navigate itself

Some context is inherently reactive — the agent discovers it during work.
Sub-agents need a lightweight spec-driver literacy ("tourist mode") to:

- **Access memories**: Query `spec-driver list memories -p <path>` for
  files discovered during implementation (not just pre-known ones)
- **Create/update memories**: Record durable gotchas or patterns found
  during work (`spec-driver create memory`)
- **Update artefacts**: Mark phase sheet tasks in-progress/completed,
  append to notes
- **Recognise governance boundaries**: Know when something looks like a
  policy/ADR concern and flag it in the return summary rather than
  improvising past it
- **Lint, test, commit**: Follow project policy (injected by orchestrator)

Sub-agents do NOT need to:
- Run `/boot`, `/using-spec-driver`, or other routing ceremony
- Make governance decisions (ADR creation, policy interpretation)
- Create or scope deltas/revisions
- Run `/consult` (non-interactive — impossible)

#### 3.6.4 "Tourist" skill for sub-agents

A dedicated lightweight skill (e.g. `dispatch-worker`) gives sub-agents
just enough spec-driver fluency:

```
You are a dispatch worker executing tasks within a spec-driver project.

## Your capabilities
- Read and update phase sheet task statuses
- Append to delta/phase notes
- Query memories: `spec-driver list memories -p <path>`
- Create memories: `spec-driver create memory "<name>" --type gotcha`
- Record AC evidence in phase notes

## Your boundaries
- Do NOT create deltas, revisions, audits, or specs
- Do NOT interpret governance artefacts — flag concerns in your summary
- Do NOT modify files outside your assigned scope without documenting why
- If you hit ambiguity: document it and continue with your best judgement.
  The orchestrator will review.

## Completion protocol
Return a structured summary:
- Tasks completed (with status)
- AC evidence per task
- Files changed
- Test results (must pass before reporting complete)
- Lint results (must be clean)
- Blockers, deferred items, or governance concerns
- Memories created or updated
```

This skill is installed claude-only alongside `/dispatch`. Sub-agents
don't invoke it as a slash command — the orchestrator injects it into
their prompt directly.

#### 3.6.5 Token budget constraint

Measured from current repo state (2026-04-16), a full spec-driver boot
consumes ~23k tokens:

| Source | Tokens | Sub-agent? | Notes |
|--------|--------|-----------|-------|
| System prompt | 6.1k | unavoidable | Platform overhead |
| System tools | 7.2k | unavoidable | Platform overhead |
| CLAUDE.md + .claude/rules/ | 2.7k | yes — auto-loaded | Lint/test/commit policy; needed |
| Preboot context (governance, glossary, workflow) | 2.7k | no | Orchestrator extracts relevant bits |
| Skills metadata (all 22+) | 1.8k | ~0.1k | Worker needs only dispatch-worker |
| Messages / conversation | 2.1k | 0 | Fresh agent |

**Irreducible platform floor**: ~16k (system prompt + tools + CLAUDE.md/rules).

Full boot adds ~7k of static tokens, but those tokens contain directives that
trigger cascading actions (invoke `/using-spec-driver`, list governance, read
ADRs/policies/standards). Realistic all-in cost of full boot: **7k + 15-30k
of triggered reads and routing**. This is what the tourist path must avoid.

**Target: 2-5k spec-driver overhead** for dispatch-worker. Achieved by:
- Reusing existing components (memory CLI, phase CLI) not reinventing
- No routing ceremony (`/using-spec-driver`, `/boot`, skill routing)
- No governance listing directives (orchestrator pre-digests relevant bits)
- Only the commands and patterns sub-agents actually need

**Effective sub-agent budget**: ~120k target − 16k platform − 2-5k tourist
− Nk orchestrator context ≈ **~95-100k for implementation work**.

The orchestrator should track its total injected context size (tourist +
task context + pre-fetched memories + policy digest) and warn if it exceeds
~20k tokens (leaving <85k for work).

#### 3.6.6 Risk: context gap

The biggest residual risk is that the orchestrator fails to include a
relevant policy, memory, or governance constraint. Mitigations:

- Sub-agents in worktrees have full repo access (including `.claude/rules/`
  which Claude Code auto-loads)
- Tourist skill instructs agents to query memories for discovered files
- Orchestrator pre-fetches memories for known file scopes
- Sub-agents flag governance-looking concerns rather than resolving them
- Phase boundary review catches policy violations before merge

### 3.7 Review Strategy

**Default**: Batch review at phase boundary.

After all batches in a phase complete, orchestrator:
1. Collects AC evidence summaries from each sub-agent
2. Reviews against phase exit criteria
3. Flags gaps or failures
4. Reports to user

**Tighter pass**: When phase sheet marks ACs as `review: tight` or tasks have
tight cross-dependencies, orchestrator reviews per-batch completion before
dispatching dependent batches.

**AC evidence flow**: Sub-agents record evidence in artefacts (phase notes,
verification blocks) AND return a structured summary to the orchestrator.
The orchestrator aggregates these into phase-level evidence.

## 4. Code Impact Summary

| Path | Current State | Target State |
|------|--------------|--------------|
| `.spec-driver/skills/dispatch/SKILL.md` | does not exist | Orchestrator skill definition |
| `.spec-driver/skills/dispatch-worker/SKILL.md` | does not exist | Tourist-mode sub-agent skill |
| `.spec-driver/skills.allowlist` | 22 skills | 24 skills (+ dispatch, dispatch-worker) |

No Python code changes. No existing skill modifications.

## 5. Verification Alignment

| Verification | Impact | Notes |
|-------------|--------|-------|
| VH-001 | new | Manual: invoke on multi-phase IP, verify parallel dispatch |
| VH-002 | new | Manual: verify worktree isolation (no cross-agent conflicts) |
| VH-003 | new | Manual: verify model routing (sonnet/opus assignment) |
| VA-001 | new | Agent review: skill format compliance |

## 6. Supporting Context

- **Reference**: [lazyspec build skill](https://github.com/jkaloger/lazyspec/blob/main/skills/build/SKILL.md) — sequential per-task dispatch, two-stage review, opus for implementation / sonnet for review
- **Related Specs**: PROD-011 (Implementation Execution Workflow), PROD-016 (Agentic SDLC Configurator)
- **Claude Code capabilities**: `Agent` tool supports `isolation: "worktree"` and `model: "sonnet"|"opus"|"haiku"` parameters natively

## 7. Design Decisions & Trade-offs

### DEC-132-01: Batched dispatch over per-task dispatch

Per-task dispatch (lazyspec approach) wastes ~10-20k tokens on boot/context per
agent. Batching amortises this cost. Trade-off: less isolation between tasks in
a batch, but tasks in the same phase typically share context anyway.

### DEC-132-02: Worktree isolation via Agent tool

Claude Code's native `isolation: "worktree"` handles worktree lifecycle. No
custom git worktree management needed. Trade-off: less control over worktree
naming/location, but simplicity wins for a skill-only deliverable.

### DEC-132-03: Peer to /execute-phase

`/dispatch` is chosen instead of `/execute-phase` for parallelizable work, not
layered on top. Rationale: `/execute-phase` assumes single-agent, sequential
execution with full ceremony. Wrapping it would fight its assumptions.

### DEC-132-04: Lightweight sub-agent instructions

Sub-agents get condensed context, not full `/boot` → `/using-spec-driver` →
`/execute-phase` chains. Full ceremony per sub-agent would consume most of the
token budget on workflow overhead. Trade-off: sub-agents may miss edge-case
governance, but the orchestrator handles governance at the coordination level.

### DEC-132-05: Adaptive review granularity

Default batch review at phase boundary keeps overhead low. Tighter per-batch
review activates when phase sheet signals it. Trade-off: default may miss
inter-batch integration issues, but phase boundary review catches these.

### DEC-132-06: Tourist-mode skill for sub-agents

Sub-agents get a dedicated `dispatch-worker` skill that provides lightweight
spec-driver fluency: memory access/creation, artefact updates, governance
boundary awareness. This is injected into the sub-agent prompt by the
orchestrator, not invoked as a slash command. The key insight is that some
context is reactive (discovered during work) so the sub-agent needs enough
literacy to fish for itself — but not enough rope to make governance decisions.

### DEC-132-07: Sub-agents cannot escalate — non-interactive by design

Sub-agents (via Claude Code `Agent` tool) are non-interactive: they cannot
prompt the user. When a sub-agent hits ambiguity, it documents the blocker in
its return summary. The orchestrator surfaces unresolved blockers to the user
after batch completion. This is a hard constraint of the platform, not a choice.

### DEC-132-08: Auto-merge on green

Completed worktree branches merge back into the orchestrator's branch as soon
as checks pass. The orchestrator attempts the merge; if conflicts arise, it
reports them to the user rather than retrying or forcing.

## 8. Resolved Questions

- **Sub-agent escalation**: Not possible — `Agent` tool is non-interactive.
  Sub-agents document blockers in return summaries; orchestrator surfaces them.
- **Phase sheet fields**: Orchestrator infers from existing structure for now.
  Explicit annotations (`parallelizable`, `complexity`, `touches`, `review`)
  backlogged as IMPR for `/plan-phases` to produce. See IMPR-028.
- **Merge strategy**: Auto-merge into orchestrator's branch when checks pass.
  Conflict resolution escalated to user.
