---
id: DR-141
slug: audit_artefact_metadata_propagation_findings_block_strict_outcome_enum_list_enrichment_de_136_child
name: "Design Revision - Audit artefact metadata propagation: findings block, strict outcome enum, list enrichment (DE-136 child)"
created: "2026-05-09"
updated: "2026-05-28"
status: draft
kind: design_revision
aliases: []
owners: []
relations:
  - type: implements
    target: DE-141
delta_ref: DE-141
source_context:
  - DR-136 §9 (canonical audit placement table)
  - DE-138 (per-artefact precedent pattern)
  - ADR-010 (placement heuristic)
  - ADR-008 (lifecycle-truth reconciliation)
code_impacts:
  - path: supekku/scripts/lib/blocks/audit_findings.py
    change: "NEW: render, extract, load_audit_findings (canonical dual-path loader)"
  - path: supekku/scripts/lib/core/frontmatter_metadata/audit.py
    change: cut findings from FM field list; confirm audit_window optional
  - path: supekku/scripts/lib/changes/audit_check.py
    change: "add audit_findings_summary(); relocate audited_delta_ids from CLI; switch to load_audit_findings()"
  - path: supekku/scripts/lib/changes/artifacts.py
    change: "add mode, delta_ref optional fields to ChangeArtifact; populate from FM at load time"
  - path: supekku/scripts/lib/validation/validator.py
    change: "switch fm.get('findings') calls at :506,:608 to load_audit_findings(); gate existing checks on self.strict"
  - path: supekku/scripts/lib/formatters/change_formatters.py
    change: add format_audit_list_row(), format_audit_list_table()
  - path: supekku/scripts/lib/formatters/column_defs.py
    change: add AUDIT_COLUMNS
  - path: supekku/cli/list/reviews.py
    change: wire AUDIT_COLUMNS + summary context via format_audit_list_table
  - path: supekku/cli/list/deltas.py
    change: remove _collect_audited_delta_ids (moved to domain)
  - path: spec_driver/migrations/v0_10_0_004_audit_findings/migration.py
    change: new migration step — findings FM → block
  - path: supekku/templates/audit.md
    change: findings in block template instead of FM
verification_alignment:
  - id: VT-141-BLOCK-001
    impact: new
    notes: findings block round-trips through render → parse
  - id: VT-141-BLOCK-002
    impact: new
    notes: empty findings list renders findings:[] block
  - id: VT-141-BLOCK-003
    impact: new
    notes: duplicate blocks → error; malformed YAML → error
  - id: VT-141-BLOCK-004
    impact: new
    notes: block audit field must match artifact id
  - id: VT-141-STRICT-001
    impact: new
    notes: invalid outcome enum → error under strict; pass under non-strict
  - id: VT-141-STRICT-002
    impact: new
    notes: completed audit + undisposed finding → error under strict
  - id: VT-141-STRICT-003
    impact: new
    notes: closure_override without rationale → error under strict
  - id: VT-141-STRICT-004
    impact: new
    notes: closure_override.effect escalation → error under strict
  - id: VT-141-STRICT-005
    impact: new
    notes: duplicate finding IDs across blocks → error under strict
  - id: VT-141-TRANSITION-001
    impact: new
    notes: load_audit_findings returns findings from block when both FM and block exist
  - id: VT-141-TRANSITION-002
    impact: new
    notes: load_audit_findings falls back to FM when no block present
  - id: VT-141-LIST-001
    impact: new
    notes: list audits table renders Mode, Delta, Findings, Disposed columns
  - id: VT-141-LIST-002
    impact: new
    notes: audit_findings_summary computes correct breakdown from block
  - id: VT-141-LIST-003
    impact: new
    notes: collect_audited_delta_ids returns same results from domain as old CLI inline
  - id: VT-141-LIST-004
    impact: new
    notes: list audits JSON output includes mode, delta_ref, findings summary fields
  - id: VT-141-LIST-005
    impact: new
    notes: list audits TSV output includes enriched fields in correct order
  - id: VT-141-LIST-006
    impact: new
    notes: zero-findings audits render em-dash in findings/disposed columns
  - id: VT-141-MIGRATE-001
    impact: new
    notes: migration step produces valid block from FM findings array
  - id: VT-141-MIGRATE-002
    impact: new
    notes: migration step writes drift entry for invalid outcome enum (no auto-fix)
  - id: VT-141-MIGRATE-003
    impact: new
    notes: migration step cuts universal FM keys
  - id: VT-141-MIGRATE-004
    impact: new
    notes: migration is idempotent — file with existing block is skipped
  - id: VT-141-MIGRATE-005
    impact: new
    notes: migration handles file with both FM findings and block (block wins, FM cut)
  - id: VA-141-CORPUS-001
    impact: new
    notes: post-migration validate workspace --kind audit clean (modulo known drift)
design_decisions:
  - id: DEC-141-01
    summary: findings summary computation lives in domain (audit_check.py), not CLI or formatter
  - id: DEC-141-02
    summary: audited_delta_ids relocated from CLI to audit_check.py (cleanup, same pattern)
  - id: DEC-141-03
    summary: audit_window stays optional (F-C resolution)
  - id: DEC-141-04
    summary: per-finding outcome is the only outcome surface — no artefact-level outcome rollup (F-D resolution)
  - id: DEC-141-05
    summary: "migration step is v0_10_0_004 (DR-136 §9.5 refers to 'v05_audit_findings.py' — ordinal name; DR-141 authoritative on folder name per DE-137 convention)"
  - id: DEC-141-06
    summary: "canonical dual-path loader load_audit_findings() in blocks/audit_findings.py; all callers must use it during transition"
  - id: DEC-141-07
    summary: "ChangeArtifact grows optional mode + delta_ref fields; populated from FM at load time"
open_questions: []
---

# DR-141 – Audit artefact metadata propagation

## 1. Executive Summary

- **Delta**: [DE-141](./DE-141.md)
- **Status**: draft
- **Last Updated**: 2026-05-28
- **Synopsis**: Move audit `findings` from frontmatter array to `supekku:audit.findings@v1` block, tighten strict enforcement, enrich `list audits` columns, and provide migration step — following DE-138 per-artefact precedent.

## 2. Problem & Constraints

- **Current Behaviour**: Audit findings live as a rich FM array (`findings: [{id, description, outcome, disposition, ...}]`). `list audits` shows generic change columns (ID, Name, Tags, Status) with no audit-specific detail. Outcome/disposition validation is warn-level, not error.
- **Drivers**: DR-136 §9 specifies exact placement table, strict enforcement rules, list enrichment columns, and migration shape. F-C (audit_window optional) and F-D (per-finding outcome only) inherited from DE-136 notes.
- **Constraints**:
  - DEC-138-12: migration modules import only stdlib + `spec_driver.migrations._helpers` + `_protocol` + yaml. No `supekku.*` imports.
  - POL-003: formatters must not contain business logic; domain functions compute summaries.
  - ADR-008: lifecycle-truth doctrine — audit findings represent observed evidence, not normative state.
- **Out of Scope**: Disposition matrix redesign. New audit modes. Body-prose excision of legacy Findings sections. Audit lifecycle changes.

## 3. Block Reshape — findings FM → block

### 3.1 Block schema: `supekku:audit.findings@v1`

```yaml
schema: supekku.audit.findings
version: 1
audit: AUD-027
findings:
  - id: F-001
    description: "Delta frontmatter uses deprecated 'lifecycle' key"
    outcome: drift
    linked_issue: ISSUE-055
    linked_delta: ""
    disposition:
      status: reconciled
      kind: spec_patch
      refs:
        - kind: revision
          ref: RE-042
      rationale: "Resolved via RE-042 spec patch"
```

Same per-finding payload shape as current FM array, with block envelope fields (`schema`, `version`, `audit`) added. **Invariant**: block `audit` field MUST equal the owning artifact's `id` — validated by block parser and by strict-mode checks (VT-141-BLOCK-004).

Fields per finding:

| Field | Type | Required | Notes |
|-------|------|----------|-------|
| `id` | string | yes | Finding ID (e.g. `F-001`) |
| `description` | string | yes | |
| `outcome` | enum | yes | `aligned \| drift \| risk` — strict |
| `linked_issue` | string | no | |
| `linked_delta` | string | no | |
| `disposition` | object | no | Required for completed audits under strict |

Disposition sub-schema (unchanged from current `audit.py`):

| Field | Type | Required | Notes |
|-------|------|----------|-------|
| `status` | enum | yes | `reconciled \| accepted \| pending` |
| `kind` | enum | yes | `aligned \| spec_patch \| revision \| follow_up_delta \| follow_up_backlog \| tolerated_drift` |
| `refs` | array | no | `[{kind, ref}]` |
| `drift_refs` | array | no | `[{kind, ref}]` |
| `rationale` | string | no | |
| `closure_override` | object | no | `{effect: warn\|none, rationale: string}` — both required when present |

### 3.2 Block rendering, parsing, and loading

New module: `supekku/scripts/lib/blocks/audit_findings.py`

Functions:

- `render_audit_findings_block(audit_id, findings) -> str` — renders code-fenced block
- `extract_audit_findings(body) -> dict | None` — parses block from body text
- `load_audit_findings(path_or_body, fm=None) -> list[dict]` — **canonical dual-path loader (DEC-141-06)**

Extractor semantics (follows `blocks/spec_requirements.py` and `blocks/delta_relationships.py`):

- Returns typed `dict` with `audit`, `findings` keys, or `None` if no block found
- Duplicate blocks → `ValueError` (not silent merge)
- Malformed YAML → `ValueError` (not `None`)
- Non-mapping payload → `ValueError`
- Block `audit` field mismatch vs artifact id → `ValueError`
- Registration via module import side-effect in block schema registry

**Canonical loader `load_audit_findings()`** (DEC-141-06):

1. Try `extract_audit_findings(body)` — block-first authority
2. If no block, fall back to `fm.get("findings", [])` when `fm` provided
3. After migration sweep + FM cut phase, FM fallback path becomes dead code (remove in FM cut phase)

All callers that currently read findings must switch to this loader:

| Caller | File:line | Current |
|--------|-----------|---------|
| `collect_gating_findings` | `audit_check.py:241` | `fm.get("findings", [])` |
| `_validate_audit_disposition` | `validator.py:506` | `fm.get("findings", [])` |
| audit collision check | `validator.py:608` | `fm.get("findings", [])` |

### 3.3 FM changes

Remove `findings` from `AUDIT_FRONTMATTER_METADATA` field list. The metadata class retains `mode`, `delta_ref`, `spec_refs`, `prod_refs`, `code_scope`, `audit_window` as FM fields.

**Phase ordering constraint**: FM `findings` field cut MUST happen after migration sweep. During the transition window (block parser deployed, migration not yet applied to all audits), callers use dual-path reads: check block first, fall back to FM. This applies to `collect_gating_findings` and `audit_findings_summary`.

**DEC-141-03**: `audit_window` stays optional (F-C). No change from current metadata — confirming intent.

**DEC-141-04**: Per-finding `outcome` is the only outcome surface (F-D). No artefact-level `outcome` field. AUD-012's `outcome: pass` is a per-finding value that becomes a strict-mode error after migration.

## 4. Strict Enforcement

Tightening already-coded validation in `audit.py` / `validator.py`:

| Rule | Current | Target |
|------|---------|--------|
| Invalid `outcome` enum (per-finding) | warn | **error** under strict |
| Disposition `status × kind` invalid pair | warn | **error** under strict |
| Disposition `outcome × kind` invalid pair | warn | **error** under strict |
| Completed audit + undisposed finding | pass | **error** under strict |
| `closure_override.effect` ≥ derived gate | unchecked | **error** under strict |
| `closure_override` present without `rationale` | unchecked | **error** under strict |

Implementation targets in `validator.py`:

- `_validate_audit_disposition()` (line ~489): already emits findings for disposition matrices. Gate severity on `self.strict` — warn when non-strict, error when strict.
- Audit collision check (line ~608): already detects duplicate finding IDs. Gate severity similarly.
- New check in same validation path: completed audit + any finding with `disposition is None` or `disposition.status == "pending"` → error under strict.
- New check: `closure_override.effect` must be strictly less than derived gate effect (use existing `_EFFECT_SEVERITY` from `audit_check.py`). Missing `rationale` when `closure_override` present → error under strict.
- All existing checks remain as warnings when `self.strict` is False — no behaviour change for non-strict mode.

## 5. List Enrichment

### 5.0 ChangeArtifact model extension (DEC-141-07)

`ChangeArtifact` in `artifacts.py` grows two optional fields:

```python
mode: str | None = None     # audit-specific: conformance | discovery
delta_ref: str | None = None  # audit-specific: target delta ID
```

Populated from FM at load time in `load_change_artifact()` — same pattern as `audit_gate`. Only meaningful for `kind == "audit"` artifacts; `None` for deltas/revisions.

Source of truth for `mode`/`delta_ref` in list output: `artifact.mode` / `artifact.delta_ref` (already loaded, no file re-read needed).

### 5.1 New column definitions

New `AUDIT_COLUMNS` in `formatters/column_defs.py`:

```python
AUDIT_COLUMNS = [
  ColumnDef(label="ID", field="id", style_key="change.id"),
  ColumnDef(label="Name", field="name"),
  ColumnDef(label="Status", field="status"),
  ColumnDef(label="Mode", field="mode"),
  ColumnDef(label="Delta", field="delta_ref"),
  ColumnDef(label="Findings", field="findings"),
  ColumnDef(label="Disposed", field="disposed"),
]
```

### 5.2 Findings summary — domain layer (DEC-141-01)

New in `changes/audit_check.py`:

```python
@dataclass(frozen=True)
class AuditFindingsSummary:
  total: int
  aligned: int
  drift: int
  risk: int
  disposed: int

  def findings_cell(self) -> str:
    """DR-136 §9.4: '7 (3a/2d/2r)' or '–'."""
    if not self.total:
      return "–"
    return f"{self.total} ({self.aligned}a/{self.drift}d/{self.risk}r)"

  def disposed_cell(self) -> str:
    """DR-136 §9.4: '5/7' or '–'."""
    if not self.total:
      return "–"
    return f"{self.disposed}/{self.total}"


def audit_findings_summary(artifact: ChangeArtifact) -> AuditFindingsSummary:
  """Compute findings breakdown from block (preferred) or FM fallback."""
  ...
```

### 5.3 Audited-delta-ids relocation (DEC-141-02)

Move `_collect_audited_delta_ids()` from `supekku/cli/list/deltas.py` → `changes/audit_check.py` as `collect_audited_delta_ids(root)`. CLI becomes thin caller.

### 5.4 Formatter

New `format_audit_list_row(artifact, summary) -> dict` in `change_formatters.py`:

```python
def format_audit_list_row(
  artifact: ChangeArtifact,
  summary: AuditFindingsSummary,
) -> dict[str, str]:
  return {
    "id": artifact.id,
    "name": _format_display_name(artifact),
    "status": artifact.status,
    "mode": _format_mode_glyph(artifact),    # C or D
    "delta_ref": _format_delta_ref(artifact), # truncated
    "findings": summary.findings_cell(),
    "disposed": summary.disposed_cell(),
  }
```

### 5.5 CLI wiring

`list_audits()` in `reviews.py`:
1. Load audit artifacts via `ChangeRegistry`
2. Compute summaries: `{a.id: audit_findings_summary(a) for a in audits}` — thin wiring
3. Pass to `format_audit_list_table(audits, summaries, ...)` which uses `AUDIT_COLUMNS` + `format_audit_list_row`

## 6. Migration Step

### 6.1 Module: `spec_driver/migrations/v0_10_0_004_audit_findings/`

**DEC-141-05**: Follows `v0_10_0_003_prod_blocks`. Version key: `v0_10_0_004_audit_findings`.

Structure (DE-138 precedent):
```
v0_10_0_004_audit_findings/
├── __init__.py          # AuditFindingsStep registration
├── _emitters.py         # render_audit_findings_block (frozen-local copy)
├── migration.py         # AuditFindingsStep(BaseMigrationStep)
└── migration_test.py
```

### 6.2 Migration logic

1. **§4 universal cuts**: Remove `lifecycle`, `aliases`, `auditers`, `source` from FM (if present)
2. **Idempotence check**: If body already contains `supekku:audit.findings@v1` block → skip (return unchanged). If both FM `findings` and block exist → block wins; cut FM key only.
3. **Extract findings**: Read `findings` array from FM YAML
4. **Render block**: `render_audit_findings_block(audit_id, findings)` — code-fenced `supekku:audit.findings@v1`
5. **Insert block**: After frontmatter, before first heading (same insertion pattern as DE-138)
6. **Remove FM key**: Delete `findings` from frontmatter
7. **Drift entries**: Invalid `outcome` values (e.g. `pass`) → drift entry in `.spec-driver/drift/DL-NNN-de_141_migrate_<audit_id>.md`. Drift entry shape follows DE-138/DE-143 pattern: `{target, drift_kind, detail, disposition: open, owner: unassigned}`. File is still migrated (block written with original invalid value); drift entry records the manual fix needed.
8. **Post-write validation**: Re-parse written file, verify block present and parseable, verify `block.audit == artifact_id`

### 6.3 Cutover gate

Strict flip is gated on: no audit files remain with FM `findings` key AND all drift entries from migration have been dispositioned (or explicitly deferred). Operational check:

```
admin migrate --kind audit --dry-run  # should report 0 pending
validate workspace --kind audit --strict  # should pass
```

### 6.3 Strict flip sequence

Per DR-136 §11.3:
1. `admin migrate --kind audit --dry-run` — preview
2. `admin migrate --kind audit` — apply
3. `validate workspace --kind audit --strict` — verify
4. Manual disposition of AUD-012 etc (drift entries guide)
5. Flip `[validation.strict].audit = true` in `workflow.toml`

## 7. Body Section Reshape

Per DR-136 §9.2: Keep Observations, Recommendations, optional Evidence sections. Delete prose Findings section (block is canonical). Migration step does NOT auto-delete body prose — `validate --prune-duplicates` (future, out of scope) handles that. Migration only inserts block and cuts FM key.

## 8. Code Impact Summary

| Path | Current | Target |
|------|---------|--------|
| `supekku/scripts/lib/blocks/audit_findings.py` | does not exist | NEW: render, extract, `load_audit_findings()` canonical loader |
| `supekku/scripts/lib/core/frontmatter_metadata/audit.py` | `findings` in FM field list | CUT `findings` from FM (after migration sweep) |
| `supekku/scripts/lib/changes/artifacts.py` | no `mode`/`delta_ref` fields | add optional `mode`, `delta_ref`; populate from FM at load |
| `supekku/scripts/lib/changes/audit_check.py` | `collect_gating_findings` reads FM :241 | switch to `load_audit_findings()`; add `audit_findings_summary()`; add `collect_audited_delta_ids()` |
| `supekku/scripts/lib/validation/validator.py` | `fm.get("findings")` at :506, :608; warn-level | switch to `load_audit_findings()`; gate severity on `self.strict` |
| `supekku/scripts/lib/formatters/change_formatters.py` | no audit-specific formatter | add `format_audit_list_row()`, `format_audit_list_table()` |
| `supekku/scripts/lib/formatters/column_defs.py` | `CHANGE_COLUMNS` only | add `AUDIT_COLUMNS` |
| `supekku/cli/list/reviews.py` | uses generic `format_change_list_table` | wire `format_audit_list_table` + summary context |
| `supekku/cli/list/deltas.py` | `_collect_audited_delta_ids` inline :39 | remove; import from `audit_check` |
| `spec_driver/migrations/v0_10_0_004_audit_findings/` | does not exist | NEW: migration step |
| `supekku/templates/audit.md` | findings in FM template | findings in block template |

## 9. Verification Alignment

| ID | Impact | Description |
|----|--------|-------------|
| VT-141-BLOCK-001 | new | `render_audit_findings_block` → `extract_audit_findings` round-trip preserves all fields |
| VT-141-BLOCK-002 | new | Empty findings list renders `findings: []` block |
| VT-141-BLOCK-003 | new | Duplicate blocks → `ValueError`; malformed YAML → `ValueError` |
| VT-141-BLOCK-004 | new | Block `audit` field mismatch vs artifact id → `ValueError` |
| VT-141-STRICT-001 | new | Invalid outcome enum → error under strict; warn under non-strict |
| VT-141-STRICT-002 | new | Completed audit + undisposed finding → error under strict |
| VT-141-STRICT-003 | new | `closure_override` without `rationale` → error under strict |
| VT-141-STRICT-004 | new | `closure_override.effect` escalation beyond derived gate → error under strict |
| VT-141-STRICT-005 | new | Duplicate finding IDs within a block → error under strict |
| VT-141-TRANSITION-001 | new | `load_audit_findings` returns findings from block when both FM and block exist |
| VT-141-TRANSITION-002 | new | `load_audit_findings` falls back to FM when no block present |
| VT-141-LIST-001 | new | `list audits` table renders Mode, Delta, Findings, Disposed columns |
| VT-141-LIST-002 | new | `audit_findings_summary` computes correct breakdown from block |
| VT-141-LIST-003 | new | `collect_audited_delta_ids` returns same results from domain as old CLI inline |
| VT-141-LIST-004 | new | `list audits --json` includes mode, delta_ref, findings summary |
| VT-141-LIST-005 | new | `list audits --format=tsv` includes enriched fields in correct order |
| VT-141-LIST-006 | new | Zero-findings audits render em-dash in findings/disposed columns |
| VT-141-MIGRATE-001 | new | Migration step produces valid block from FM findings array |
| VT-141-MIGRATE-002 | new | Migration step writes drift entry for invalid outcome enum (no auto-fix) |
| VT-141-MIGRATE-003 | new | Migration step cuts universal FM keys |
| VT-141-MIGRATE-004 | new | Migration idempotent — file with existing block skipped |
| VT-141-MIGRATE-005 | new | File with both FM findings and block: block preserved, FM key cut |
| VA-141-CORPUS-001 | new | Post-migration `validate workspace --kind audit` clean (modulo known drift) |

## 10. Design Decisions

- **DEC-141-01**: Findings summary computation lives in domain (`audit_check.py`). Formatter receives pre-computed `AuditFindingsSummary`; CLI does thin wiring only.
- **DEC-141-02**: `_collect_audited_delta_ids` relocated from `cli/list/deltas.py` to `changes/audit_check.py`. Same logic, proper layer.
- **DEC-141-03**: `audit_window` stays optional (F-C resolution). Current corpus has audits without it (AUD-024). No strictness change.
- **DEC-141-04**: Per-finding `outcome` is the only outcome surface (F-D resolution). No artefact-level outcome rollup. `outcome: pass` on AUD-012 is a per-finding error.
- **DEC-141-05**: Migration step numbered `v0_10_0_004` — follows `v0_10_0_003_prod_blocks` in sequence. DR-136 §9.5 refers to `v05_audit_findings.py` (ordinal name); DR-141 is authoritative on the final folder name per DE-137 convention.
- **DEC-141-06**: Canonical dual-path loader `load_audit_findings()` lives in `blocks/audit_findings.py`. Block-first authority; FM fallback during transition. All three current FM readers (audit_check.py:241, validator.py:506, validator.py:608) must switch to this loader.
- **DEC-141-07**: `ChangeArtifact` grows optional `mode` and `delta_ref` fields. Populated from FM at load time in `load_change_artifact()`. Only meaningful for audit-kind artifacts.

## 11. References

- DR-136 §9 (canonical placement table)
- DE-138 DR/IP (per-artefact precedent)
- `supekku/scripts/lib/core/frontmatter_metadata/audit.py` (current audit metadata)
- `supekku/scripts/lib/changes/audit_check.py` (gating/finding logic)
- ADR-008 (lifecycle-truth reconciliation)
- ADR-010 (placement heuristic)
