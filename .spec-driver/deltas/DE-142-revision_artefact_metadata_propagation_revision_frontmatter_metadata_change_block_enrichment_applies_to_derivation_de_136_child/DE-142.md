---
id: DE-142
slug: revision_artefact_metadata_propagation_revision_frontmatter_metadata_change_block_enrichment_applies_to_derivation_de_136_child
name: 'Delta - Revision artefact metadata propagation: REVISION_FRONTMATTER_METADATA
  + change-block enrichment + applies_to derivation (DE-136 child)'
created: '2026-05-09'
updated: '2026-05-09'
status: draft
kind: delta
relations:
- type: relates_to
  target: DE-136
  nature: Umbrella program; this child propagates ADR-010 placement heuristic to revision
    artefacts (DR-136 §10) — last in DEC-004 sequence; benefits from four per-artefact
    precedents
- type: relates_to
  target: DE-137
  nature: Cross-cutting infrastructure provides MetadataValidator + admin migrate
    orchestrator
- type: relates_to
  target: DE-138
  nature: Per-artefact precedent — revision brought to parity with delta shape and
    rigour
- type: relates_to
  target: DE-118
  nature: REVISION_CHANGE_METADATA introduced by DE-118; this delta enriches schema
    (action enum + conditional rules)
- type: relates_to
  target: ADR-010
  nature: Placement heuristic
- type: relates_to
  target: ADR-008
  nature: Lifecycle-truth doctrine — revision-change intent vs observed reconciliation
---

# DE-142 – Revision artefact metadata propagation: REVISION_FRONTMATTER_METADATA + change-block enrichment + applies_to derivation (DE-136 child)

```yaml supekku:delta.relationships@v1
schema: supekku.delta.relationships
version: 1
delta: DE-142
revision_links:
  introduces: []
  supersedes: []
specs:
  primary:
    - PROD-004
    - SPEC-114
    - SPEC-116
  collaborators: []
requirements:
  implements:
    - PROD-004.FR-001
    - PROD-004.FR-002
  updates: []
  verifies: []
phases: []
```

## 1. Summary & Context

- **Umbrella**: [DE-136](../DE-136-metadata_schema_consolidation_program_propagate_adr_010_across_artefacts_and_close_prod_004/DE-136.md).
- **Canonical design reference**: DR-136 §10 (revision placement table + block schema enrichment + body reshape + list enrichment + `v06_revision_metadata.py`).
- **Sequence position**: last per-artefact child delta (DEC-004). Benefits from four precedents (delta, spec, requirements-in-spec, audit). Brings revision to parity with delta — symmetrical shape and rigour.
- **Product Spec(s)**: PROD-004 (FR-001/-002).
- **Technical Spec(s)**: SPEC-114 (block schema), SPEC-116 (frontmatter_metadata — `REVISION_FRONTMATTER_METADATA` introduction); revision-domain specs.
- **Implementation Plan**: TBD — authored when delta enters DE-136 Phase 3.
- **Change Drivers**: DE-136 program; revision currently uses hand-rolled FM (no `REVISION_FRONTMATTER_METADATA`); DR-136 §10 brings parity.

## 2. Motivation

_Carried by DR-136 §10; this delta's own DR-142 will restate. Frontmatter + summary only at this stage._

## 3. Scope & Objectives

- **Primary Outcomes** (DR-136 §10):
  1. **Field placement** (§10.1): hand-rolled FM → new `REVISION_FRONTMATTER_METADATA` (Base 7 + relations + tags + ext_id/ext_url); §4 universals cut; `source_specs`/`destination_specs`/`requirements` → derived `applies_to` from `supekku:revision.change@v1` block.
  2. **Block schema enrichment** (§10.2): `supekku:revision.change@v1` enriched with strict `action` enum (`introduce | retire | move | modify`) and conditional `action × field-presence` rules via `MetadataValidator`. Intent vs observed (ADR-008): block represents revision *intent*; reconciliation against actual spec state happens at revision close (audit step).
  3. **Body reshape** (§10.3): delete §§1 (Source/Destination Specs) and 3 (Requirements list); keep Rationale, Migration/Patch.
  4. **`list revisions` enrichment** (§10.4): default columns ID, Name, Status, Source (count + first), Destination (count + first), Requirements (count).
  5. **Migration** (§10.5): `migrations/v06_revision_metadata.py` — §4 cuts; if FM has source_specs/destination_specs/requirements but no block, synthesise block with `action: move` + `from_lifecycle`/`to_lifecycle: unknown` (tolerated_alias) + drift entry per record; if both FM and block present, block wins, FM keys removed; body §§1, 3 cleanup deferred to `--prune-duplicates`.
- **Operational Constraints**:
  - DE-118 must have closed (provides `REVISION_CHANGE_METADATA` foundation; this delta extends with action enum).
  - DE-137 closed (orchestrator + state).
  - All four prior per-artefact deltas (DE-138/139/140/141) closed — full inherited pattern.
  - Strict-mode flip happens after sweep produces clean validate or all errors are drift-tracked.
- **Dependencies**:
  - **Hard**: DE-118, DE-137, DE-138, DE-139, DE-140, DE-141.

## 4. Out of Scope

- Spec-state reconciliation logic (covered at revision close per ADR-008; not this delta's concern).
- Body-prose excision (`validate --prune-duplicates`).
- Cross-revision aggregation features.
- Migrating the `revision.py` extractor — DE-118 already retains the bespoke line-by-line state-machine extractor for nested fences.

## 5. Approach Overview

_Refined by DR-142._

- **System Touchpoints**: `supekku/scripts/lib/core/frontmatter_metadata/revision.py` (NEW `REVISION_FRONTMATTER_METADATA`), `supekku/scripts/lib/blocks/revision_change.py` (extend with action enum + conditional rules — DE-118 introduced the metadata vehicle), `supekku/scripts/lib/changes/` (revision artefact model — derived `applies_to` property), `supekku/scripts/lib/formatters/change_formatters.py` (`list revisions` columns), `supekku/cli/list/revisions.py`, `supekku/scripts/lib/migrations/v06_revision_metadata.py` (NEW), `supekku/templates/revision.md`.
- **Key Changes**: per the five outcomes in §3.
- **Migration / Rollout Notes**: standard sweep per DR-136 §11.2; revision corpus is auto-generated via `complete delta` flow — most legacy revisions follow predictable shape; expect low drift rate.

## 6. Verification Strategy

- **Requirements Coverage**: <How each requirement listed above will be verified>
- **Planned Artefacts**: <VT/VA/VH IDs expected from implementation plan phases>
- **Acceptance Criteria**: <Objective pass/fail gates before marking delta complete>

## 7. Follow-ups & Tracking

- **Future Phases / Deltas**: post-umbrella cleanup of revision body excision via `validate --prune-duplicates`.
- **Backlog Items**: none specific.
- **Open Decisions / Questions** (resolved by DR-142):
  - Whether `MetadataValidator` already supports the conditional-rule shape needed for `action × field-presence`, or whether DE-118 / DE-137 need to extend it.
  - Whether `from_lifecycle`/`to_lifecycle` enum reuses requirement-lifecycle enum (`pending|in-progress|active|baseline|verified|retired`) directly.

## 8. Implementation Notes

- <Environment setup, scripts, commands, coordination notes>
- <Links to design artefacts, diagrams, reference documentation>

> Replace placeholders with concrete details. Remove sections that do not apply rather than leaving empty headings.

```yaml supekku:delta.context_inputs@v1
schema: supekku.delta.context_inputs
version: 1
entries:
- type: delta
  id: DE-136
  summary: Umbrella; DR-136 §10 is the canonical placement table for revision kind,
    including REVISION_FRONTMATTER_METADATA introduction and `supekku:revision.change@v1`
    action enum enrichment
- type: delta
  id: DE-137
  summary: Cross-cutting infrastructure must close before this delta's sweep
- type: delta
  id: DE-118
  summary: Provides REVISION_CHANGE_METADATA — this delta extends, does not redefine
- type: adr
  id: ADR-010
  summary: Placement heuristic
- type: adr
  id: ADR-008
  summary: Lifecycle-truth doctrine
```

```yaml supekku:delta.risk_register@v1
schema: supekku.delta.risk_register
version: 1
risks: []
```
