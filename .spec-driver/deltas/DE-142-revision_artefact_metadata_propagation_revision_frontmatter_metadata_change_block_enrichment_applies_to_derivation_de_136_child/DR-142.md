---
id: DR-142
slug: revision_artefact_metadata_propagation_revision_frontmatter_metadata_change_block_enrichment_applies_to_derivation_de_136_child
name: "Design Revision - Revision artefact metadata propagation: REVISION_FRONTMATTER_METADATA + change-block enrichment + applies_to derivation (DE-136 child)"
created: "2026-05-09"
updated: "2026-05-29"
status: draft
kind: design_revision
aliases: []
owners: []
relations:
  - type: implements
    target: DE-142
delta_ref: DE-142
source_context: []
code_impacts:
  - path: supekku/scripts/lib/blocks/metadata/schema.py
    current_state: "conditional_rules exists on BlockMetadata only"
    target_state: "add conditional_rules: list[ConditionalRule] to FieldMetadata (additive; default empty)"
  - path: supekku/scripts/lib/blocks/metadata/validator.py
    current_state: "conditional rules applied at top level only"
    target_state: "apply object-level field_meta.conditional_rules in _validate_object (covers nested objects + array items)"
  - path: supekku/scripts/lib/blocks/revision_metadata.py
    current_state: "action × field-presence conditionality is prose-only in field descriptions"
    target_state: "declare conditional_rules on requirements[] item: move→{origin,destination}, introduce/modify→{destination}"
  - path: supekku/scripts/lib/core/frontmatter_metadata/revision.py
    current_state: "DE-137 stub: Base + status enum only"
    target_state: "complete REVISION_FRONTMATTER_METADATA: Base 7 + relations + tags + ext_id/ext_url"
  - path: supekku/scripts/lib/changes/artifacts.py
    current_state: "no revision applies_to derivation; FM source_specs/destination_specs/requirements read directly"
    target_state: "revision-block applies_to deriver (specs[]+requirements[]); hook load_change_artifact for kind:revision; FM scope keys no longer read"
  - path: supekku/scripts/lib/changes/revision_check.py
    current_state: "does not exist"
    target_state: "NEW: RevisionChangeSummary + revision_change_summary() — domain summary for list (mirror audit_check.py)"
  - path: supekku/scripts/lib/formatters/column_defs.py
    current_state: "no REVISION_COLUMNS"
    target_state: "add REVISION_COLUMNS (ID, Name, Status, Source, Destination, Requirements)"
  - path: supekku/scripts/lib/formatters/change_formatters.py
    current_state: "no revision-specific formatter"
    target_state: "add format_revision_list_row(), format_revision_list_table()"
  - path: supekku/cli/list/reviews.py
    current_state: "list revisions uses generic change table"
    target_state: "wire REVISION_COLUMNS + summary context via format_revision_list_table"
  - path: spec_driver/migrations/v0_10_0_005_revision_metadata/
    current_state: "does not exist"
    target_state: "NEW migration step — §4 universal cuts + FM scope-key cut + block synthesis for FM-only records"
  - path: supekku/templates/revision.md
    current_state: "hand-rolled FM keys (source_specs/destination_specs/requirements/aliases)"
    target_state: "FM template: cut hand-rolled + universal keys; block canonical"
verification_alignment:
  - verification: VT-142-ENGINE-001
    impact: new
  - verification: VT-142-ENGINE-002
    impact: new
  - verification: VT-142-ENGINE-003
    impact: new
  - verification: VT-142-BLOCK-001
    impact: new
  - verification: VT-142-BLOCK-002
    impact: new
  - verification: VT-142-BLOCK-003
    impact: new
  - verification: VT-142-FM-001
    impact: new
  - verification: VT-142-FM-002
    impact: new
  - verification: VT-142-DERIVE-001
    impact: new
  - verification: VT-142-DERIVE-002
    impact: new
  - verification: VT-142-LIST-001
    impact: new
  - verification: VT-142-LIST-002
    impact: new
  - verification: VT-142-LIST-003
    impact: new
  - verification: VT-142-LIST-004
    impact: new
  - verification: VT-142-MIGRATE-001
    impact: new
  - verification: VT-142-MIGRATE-002
    impact: new
  - verification: VT-142-MIGRATE-003
    impact: new
  - verification: VT-142-MIGRATE-004
    impact: new
  - verification: VA-142-CORPUS-001
    impact: new
design_decisions:
  - id: DEC-142-01
    summary: "block shape unchanged — DE-118 rich metadata/specs/requirements is canonical; DE-142 is additive only (F-F). DR-136 §10.2 flat sketch reconciled via erratum 2026-05-29"
  - id: DEC-142-02
    summary: "conditional rules enforced by extending FieldMetadata with conditional_rules + applying in _validate_object (declarative, metadata-driven, reusable) — not hand-rolled per-item checks"
  - id: DEC-142-03
    summary: "conditional rules scoped to requirements[] action only (introduce/modify→destination, move→origin+destination); specs[] action unconstrained (§10.2 intent)"
  - id: DEC-142-04
    summary: "from/to_lifecycle resolved by reality — existing block lifecycle.status reuses REQUIREMENT_STATUSES; no new enum. unknown is migration tolerated_alias only"
  - id: DEC-142-05
    summary: "applies_to derived from block, never stored in FM (DEC-138-10 no-competing-truths); source_specs/destination_specs/requirements cut from FM"
  - id: DEC-142-06
    summary: "REVISION_FRONTMATTER_METADATA completes the DE-137 stub — plain relations (mirror audit), no revision_links-derived projection (that is a delta concern)"
  - id: DEC-142-07
    summary: "migration folder v0_10_0_005_revision_metadata — follows v0_10_0_004_audit_findings; DR-136 §10.5 'v06' is ordinal, DR-142 authoritative on folder name (per DEC-141-05)"
open_questions: []
---

# DR-142 – Revision artefact metadata propagation

## 1. Executive Summary

- **Delta**: [DE-142](./DE-142.md)
- **Status**: draft
- **Last Updated**: 2026-05-29
- **Synopsis**: Complete the revision frontmatter metadata class, enforce the
  `supekku:revision.change@v1` block's `action × field-presence` rules
  declaratively, derive `applies_to` from the block (cutting hand-rolled FM
  keys), enrich `list revisions`, and ship the `v0_10_0_005` migration. Last
  per-artefact child under DE-136; inherits the DE-138/DE-141 precedent. The
  block schema is **not** redefined — DE-142 is additive over DE-118 (F-F).

## 2. Problem & Constraints

- **Current Behaviour**:
  - `REVISION_FRONTMATTER_METADATA` is a DE-137 stub (Base + status enum only;
    docstring: "full schema is DE-142 scope"). Real revisions still carry
    hand-rolled FM keys (`source_specs`, `destination_specs`, `requirements`,
    `aliases`).
  - `supekku:revision.change@v1` (shipped by DE-118) already has the
    requirement-level `action` enum (`introduce | modify | move | retire`) and
    rich `specs`/`requirements` payloads, validated by `MetadataValidator`. But
    the `action × field-presence` conditionality is **prose-only** in field
    descriptions — not enforced. `ConditionalRule` exists but only traverses
    top-level dot-paths; it cannot reach `requirements[].action`.
  - `list revisions` shows generic change columns; no source/destination/
    requirement detail.
- **Drivers**: DR-136 §10 (reconciled placement table); F-F scope-note
  (DE-136 `notes.md`): "additive enrichment, not redefinition" of the DE-118
  block. PROD-004 FR-001/FR-002.
- **Constraints / Guardrails**:
  - **F-F (load-bearing)**: extend, do not redefine, `REVISION_CHANGE_METADATA`.
    DR-136 §10.2's earlier *flat* `changes:` sketch was a presentation defect;
    corrected in place via erratum (2026-05-29). The shipped rich schema is
    canonical. **No `@v2`, no corpus block re-shape.**
  - **DEC-138-10 (no competing truths)**: `applies_to` is derived from the
    block at load, never persisted in FM. Storing it alongside the block is a
    strict-mode error.
  - **DE-118 §11.1-equivalent boundary** (DR-136 §11.1): the metadata engine is
    application code (current-schema only); migration code is legacy-aware and
    lives in `spec_driver/migrations/`, importing only stdlib + migration
    helpers. The engine extension here (`FieldMetadata.conditional_rules`) is
    additive application code, used by all blocks.
  - POL-003: formatters carry no business logic; domain computes summaries.
  - ADR-008: the block is revision *intent*; reconciliation against observed
    spec state is a revision-close concern, not this delta's.
- **Out of Scope**:
  - Block top-level reshape (no `@v2`).
  - Spec-state reconciliation at revision close.
  - Body-prose excision (`validate --prune-duplicates`).
  - Migrating the `blocks/revision.py` line-by-line extractor (DE-118 retains it
    for nested fences).

## 3. Architecture Intent

- **Target Outcomes**:
  1. Revision FM is governed by a complete `REVISION_FRONTMATTER_METADATA`
     equal in rigour to delta/audit (FR-001/FR-002).
  2. The change block's `action × field-presence` invariant is **declared**, not
     prose, and enforced under strict.
  3. `applies_to` is the single derived view of spec/requirement scope; FM stops
     carrying competing copies.
  4. `list revisions` surfaces scope at a glance.
  5. Legacy revisions migrate cleanly or are drift-tracked; strict flips.
- **Guiding Principles**: additive over DE-118; declarative metadata over
  hand-rolled validation; derive-don't-duplicate; reuse the DE-141 per-artefact
  shape (block → domain summary → formatter column → migration → strict flip).
- **Lifecycle Impact**: `[validation.strict].revision = true` at close. No
  change to revision status enum (inherited from change-status via DE-137).

## 4. Block conditional-rule enforcement (additive — DEC-142-02)

### 4.1 Engine extension

`ConditionalRule` (top-level only today) cannot express per-item rules because
`_validate_conditional_rules` walks `data` via dot-paths and never iterates
arrays. Extend the schema so any **object** can carry its own rules:

```python
# schema.py — FieldMetadata gains (additive, default empty):
conditional_rules: list[ConditionalRule] = field(default_factory=list)
```

```python
# validator.py — _validate_object, after property validation:
if field_meta.conditional_rules:
  errors.extend(self._apply_conditional_rules(value, field_meta.conditional_rules, field_path))
```

`_apply_conditional_rules(obj, rules, path)` is the existing
`_validate_conditional_rules` body, generalised to take an explicit object +
rule list + path prefix (so item errors read `requirements[2].origin`). The
top-level `validate()` call delegates to the same helper with
`self.metadata.conditional_rules` and `path=""` — **no behaviour change** for
blocks that declare none (VT-142-ENGINE-003).

Because array items dispatch `_validate_field → _validate_object`, declaring
rules on the `requirements[]` *item* `FieldMetadata` makes them fire per element
with `action` resolved as a direct key of each item (`_get_nested_value(item,
"action")`).

### 4.2 Declared rules (revision_metadata.py)

On the `requirements[]` item object `FieldMetadata`:

```python
conditional_rules=[
  ConditionalRule("action", "move",      ["origin", "destination"],
    description="move requires both origin and destination"),
  ConditionalRule("action", "introduce", ["destination"],
    description="introduce requires destination"),
  ConditionalRule("action", "modify",    ["destination"],
    description="modify requires destination"),
]
```

`retire` adds no requirement (DEC-142-03). `specs[]` `action` stays
unconstrained — §10.2 intent is requirement-level.

**Intent vs observed (ADR-008)**: these enforce well-formed *intent*
(structurally complete change descriptions), not agreement with current spec
state.

## 5. Frontmatter completion (DEC-142-06)

`REVISION_FRONTMATTER_METADATA` moves from the DE-137 stub to:

| Field group | Fields | Source |
|---|---|---|
| Base 7 | `id`, `name`, `slug`, `kind`, `status`, `created`, `updated` | BASE (status keeps revision enum + aliases) |
| Relations | `relations` (plain `{type, target, nature?}`) | BASE; mirror audit, **no** `revision_links` projection |
| Tags | `tags` | BASE |
| External | `ext_id`, `ext_url` | BASE (optional) |

Cut (universal §4): `lifecycle`, `aliases`, `auditers`, `source`. Cut
(hand-rolled): `source_specs`, `destination_specs`, `requirements` — these
become derived `applies_to` (§6). `owners`/`summary` stay in BASE but are
omitted from the template.

After-shape example (RE-042) — see DE-142 §"Worked example".

## 6. `applies_to` derivation (DEC-142-05)

Mirror `_derive_applies_to` (DR-138 §6.1 / DEC-138-10). Add a revision-block
deriver in `artifacts.py`, hooked into `load_change_artifact` for
`kind == "revision"`:

- `specs` ← unique `block.specs[].spec_id`
- `requirements` ← unique `block.requirements[].requirement_id`
- destination/source distinction (for the list columns) read from
  `requirements[].destination.spec` vs `requirements[].origin[].ref` — computed
  in the domain summary (§7), not stored.

`applies_to` is **runtime-only**. If FM contains `applies_to` (or the cut
`source_specs`/etc.) alongside a block → strict-mode error (VT-142-DERIVE-002),
matching DEC-138-10.

## 7. List enrichment

### 7.1 Domain summary (DEC-142-01) — `changes/revision_check.py` (NEW)

```python
@dataclass(frozen=True)
class RevisionChangeSummary:
  sources: list[str]        # origin spec refs (deduped, ordered)
  destinations: list[str]   # destination spec refs
  requirements: list[str]   # requirement_ids

  def source_cell(self) -> str: ...       # "2 (PROD-004)" or "–"
  def destination_cell(self) -> str: ...  # "1 (PROD-004)" or "–"
  def requirements_cell(self) -> str: ... # "3" or "–"

def revision_change_summary(artifact: ChangeArtifact) -> RevisionChangeSummary:
  """Compute scope breakdown from the revision.change block."""
```

### 7.2 Columns + formatter

`REVISION_COLUMNS` in `column_defs.py`: `ID, Name, Status, Source, Destination,
Requirements`. `format_revision_list_row(artifact, summary)` +
`format_revision_list_table(...)` in `change_formatters.py` — same dict-row
pattern as `format_audit_list_row` (DE-141 §5.4).

### 7.3 CLI wiring — `cli/list/reviews.py:list_revisions`

Load via `ChangeRegistry(kind="revision")`; compute
`{r.id: revision_change_summary(r)}`; pass to `format_revision_list_table`.
Thin orchestration only.

## 8. Migration — `v0_10_0_005_revision_metadata` (DEC-142-07)

> **Corpus-driven revision (2026-05-29, P04 consult — all 4 sub-decisions ratified).**
> The original §8 plan (synthesise `action: move` + `lifecycle.status: unknown`,
> drift per record) is unimplementable against the real corpus and superseded below.
> Drivers (verified): 97 legitimate refs across ~24 records use `PROD-*`/`ISSUE-*`
> containers and the `NF-` token, which the SPEC-only `requirement_id` regex rejects;
> zero corpus blocks use `move` (all `modify`); FM-only records are in-place
> (`source==destination`) or dest-only; `lifecycle` is optional. See notes.md session 6
> + DEC-CONSULT-03/04/05/07.

**§8.0 Pre-req — broaden ID patterns (DEC-CONSULT-04).** Application-code change (not
migration), shipped in P04 ahead of the sweep. Hoist two shared constants and reuse
across the revision + verification blocks (POL-001 — collapses 7 hardcoded SPEC-only
copies and fixes the latent `NF` gap in `verification_metadata.REQUIREMENT_ID_PATTERN`):

```python
REQUIREMENT_ID_PATTERN = r"^(SPEC|PROD|ISSUE)-\d{3,}(?:-[A-Z0-9]+)*\.(FR|NF|NFR)-[A-Z0-9.-]+$"
SPEC_ID_PATTERN        = r"^(SPEC|PROD|ISSUE)-\d{3,}(?:-[A-Z0-9]+)*$"
```

- `REQUIREMENT_ID_PATTERN` → `requirements[].requirement_id`, `destination.requirement_id`,
  `specs[].requirement_flow.{added,removed,moved_in,moved_out}[]`.
- `SPEC_ID_PATTERN` → `specs[].spec_id`, `destination.spec`, `destination.additional_specs[]`.
- `RE-`/`DE-`/`AUD-` patterns unchanged (correct).
- F-F-safe relaxation: no previously-valid block becomes invalid; not a `@v2`.

**Migration step logic** (structure follows `v0_10_0_004_audit_findings`):

1. **§4 universal cuts**: remove `lifecycle`, `aliases`, `auditers`, `source`.
2. **Hand-rolled cuts**: remove `source_specs`, `destination_specs`,
   `requirements` from FM.
3. **Idempotence**: block present + FM keys already cut → skip.
4. **Block synthesis (FM-only)**: FM has `source_specs`/`destination_specs`/
   `requirements` but no block → synthesise the rich block: `specs[]` from the
   `source_specs ∪ destination_specs` union (`action: updated`); one
   `requirements[]` entry per FM requirement with **`action: modify`**,
   `destination.spec` from `destination_specs`, **`lifecycle` omitted**, `origin`
   omitted (no `move`). Faithful, lossless reformat of FM keys → **no drift entry**.
5. **Both present**: block wins; cut FM keys; no drift.
6. **Post-write validation**: re-parse, confirm block parses and `metadata.revision`
   matches artifact id.
7. Body §§1/3 prose cleanup deferred to `validate --prune-duplicates`.

**No `unknown` tolerated_alias, no synthesis drift** (DEC-CONSULT-03/05): `lifecycle`
is optional → synthesis omits it rather than inventing `unknown`, so the shared
`REQUIREMENT_STATUSES` enum is untouched (no R-142-02 leak). Faithful synthesis is not
spec-vs-reality divergence → no DL entries. Any residual strict error after the sweep
(expected ~0) is dispositioned manually. The generic-orchestrator drift-write gap is
tracked as **ISSUE-061** (out of DE-142 scope), not built here.

### 8.1 Strict flip sequence (DR-136 §11.2)

```
spec-driver validate --kind revision                          # tolerant baseline
spec-driver admin migrate revision --dry-run                  # preview
spec-driver admin migrate revision                            # apply
spec-driver validate --kind revision --strict                 # re-validate (VA-142-CORPUS-001)
# manual disposition of any residual (expected none after §8.0 broadening)
spec-driver validate --kind revision --strict --no-tolerated-aliases  # final gate
# then MANUALLY (DEC-CONSULT-07): [validation.strict].revision = true in workflow.toml
```

## 9. Body Section Reshape

Per DR-136 §10.3: delete body §§1 (Source/Destination Specs), 3 (Requirements
list); keep Rationale, Migration/Patch. Migration inserts/keeps the block only;
prose deletion is `--prune-duplicates` (out of scope).

## 10. Code Impact Summary

| Path | Current | Target |
|------|---------|--------|
| `blocks/metadata/schema.py` | `conditional_rules` on `BlockMetadata` only | add to `FieldMetadata` (additive) |
| `blocks/metadata/validator.py` | rules applied at top level only | apply object-level rules in `_validate_object` (shared helper) |
| `blocks/revision_metadata.py` | conditionality prose-only | declare `conditional_rules` on `requirements[]` item |
| `blocks/<shared>` (e.g. `metadata/patterns.py`) | none | NEW: hoist `REQUIREMENT_ID_PATTERN` + `SPEC_ID_PATTERN` shared constants (DEC-CONSULT-04) |
| `blocks/revision_metadata.py` | 7 hardcoded SPEC-only ID regexes | reuse the two broadened shared constants |
| `blocks/verification_metadata.py` | local SPEC\|PROD, FR\|NFR `REQUIREMENT_ID_PATTERN` (no `NF`/`ISSUE`) | reuse the broadened shared constant (fixes latent `NF` gap) |
| `core/frontmatter_metadata/revision.py` | stub (Base+status) | Base 7 + relations + tags + ext_id/ext_url |
| `changes/artifacts.py` | no revision applies_to derivation | derive from block for `kind:revision`; FM keys no longer read |
| `changes/revision_check.py` | does not exist | NEW: `RevisionChangeSummary` + `revision_change_summary()` |
| `formatters/column_defs.py` | no `REVISION_COLUMNS` | add `REVISION_COLUMNS` |
| `formatters/change_formatters.py` | no revision formatter | add `format_revision_list_row/table` |
| `cli/list/reviews.py` | generic change table for revisions | wire `format_revision_list_table` + summaries |
| `spec_driver/migrations/v0_10_0_005_revision_metadata/` | does not exist | NEW migration step |
| `supekku/templates/revision.md` | hand-rolled FM keys | cut keys; block canonical |

## 11. Verification Alignment

| ID | Impact | Description |
|----|--------|-------------|
| VT-142-ENGINE-001 | new | Per-item conditional rule: `move` without `origin` → error under strict |
| VT-142-ENGINE-002 | new | Object-level rules fire for nested (non-array) objects too |
| VT-142-ENGINE-003 | new | Empty `conditional_rules` (default) → no behaviour change |
| VT-142-BLOCK-001 | new | `action=move` missing `origin`/`destination` → error |
| VT-142-BLOCK-002 | new | `action=introduce`/`modify` missing `destination` → error |
| VT-142-BLOCK-003 | new | Well-formed entries pass strict (incl. RE-042 `modify` shape) |
| VT-142-FM-001 | new | FM class accepts Base 7 + relations + tags + ext_id/ext_url |
| VT-142-FM-002 | new | Cut keys rejected as unknown under strict |
| VT-142-DERIVE-001 | new | `applies_to` derived from block specs + requirements |
| VT-142-DERIVE-002 | new | `applies_to`/cut-key in FM beside block → strict error |
| VT-142-LIST-001 | new | Table renders Source/Destination (count+first), Requirements (count) |
| VT-142-LIST-002 | new | `revision_change_summary` correct breakdown from block |
| VT-142-LIST-003 | new | Zero-change revision → em-dash cells |
| VT-142-LIST-004 | new | JSON/TSV include enriched fields, correct order |
| VT-142-PATTERN-001 | new | Broadened `REQUIREMENT_ID_PATTERN`/`SPEC_ID_PATTERN` accept `PROD-*`/`ISSUE-*` + `NF-` (e.g. `PROD-007.NF-001`, `ISSUE-016.FR-016.001`); reject garbage |
| VT-142-MIGRATE-001 | new | Migration cuts §4 + hand-rolled FM keys |
| VT-142-MIGRATE-002 | revised | FM-only → synthesised `modify` block (no `move`, `lifecycle` omitted), **no drift entry** |
| VT-142-MIGRATE-003 | new | Both present → block wins, FM cut, no drift |
| VT-142-MIGRATE-004 | new | Idempotent — migrated file skipped |
| VA-142-CORPUS-001 | new | Post-migration `validate --kind revision --strict` clean (expected zero residual after §8.0 broadening) |

## 12. Design Decisions

- **DEC-142-01**: Block shape unchanged; DE-118 rich schema canonical; additive
  only (F-F). DR-136 §10.2 flat sketch reconciled via in-place erratum
  (2026-05-29) — see DE-136 phase-03 §9.
- **DEC-142-02**: Conditional rules enforced by extending `FieldMetadata` +
  `_validate_object`, not hand-rolled per-item checks. Declarative,
  metadata-driven (honours DE-118 single-validation-layer ethos), reusable by
  any array block.
- **DEC-142-03**: Rules scoped to `requirements[].action`
  (introduce/modify→destination, move→origin+destination). `specs[]` action and
  `retire` unconstrained.
- **DEC-142-04**: `from/to_lifecycle` resolved by reality — block
  `lifecycle.status` already reuses `REQUIREMENT_STATUSES`. No new enum.
  ~~`unknown` is a migration-only tolerated_alias.~~ **Superseded by DEC-142-09
  (P04 consult, ratified 2026-05-29): no `unknown` — synthesis omits the optional
  `lifecycle` entirely. Shared `REQUIREMENT_STATUSES` untouched.**
- **DEC-142-08 (P04 consult, DEC-CONSULT-04)**: broaden block ID patterns to shared
  `REQUIREMENT_ID_PATTERN`/`SPEC_ID_PATTERN` (`(SPEC|PROD|ISSUE)` + `(FR|NF|NFR)`),
  reused across revision + verification blocks. The SPEC-only regex rejected 97
  legitimate corpus refs (`PROD-*`/`ISSUE-*` containers, `NF-` token — 99 `NF`
  requirements exist repo-wide). Pure relaxation (F-F-safe). Closest sibling (delta
  relationships block) has no ID patterns at all.
- **DEC-142-09 (P04 consult, DEC-CONSULT-03)**: synthesise `action: modify` (not
  `move`); omit `lifecycle`. Zero corpus blocks use `move`; FM-only records are
  in-place or dest-only; `move` requires `origin` (absent everywhere).
- **DEC-142-10 (P04 consult, DEC-CONSULT-05)**: no automated drift in this migration
  (faithful synthesis is lossless). Generic-orchestrator drift-write gap tracked as
  ISSUE-061. Residual strict errors dispositioned manually.
- **DEC-142-11 (P04 consult, DEC-CONSULT-07)**: `[validation.strict].revision = true`
  flipped manually after VA-142-CORPUS-001 disposition, decoupled from the migrate run.
- **DEC-142-05**: `applies_to` derived from block, never stored
  (DEC-138-10 no-competing-truths). Hand-rolled FM scope keys cut.
- **DEC-142-06**: FM completion mirrors audit (plain `relations`); no
  `revision_links`-derived relation projection (a delta concern).
- **DEC-142-07**: Migration folder `v0_10_0_005_revision_metadata` (follows
  `v0_10_0_004`); DR-136 §10.5 "v06" is ordinal — DR-142 authoritative on the
  folder name per DEC-141-05.

## 13. Adversarial review residuals & risks

Surfaced by self-review; resolved at IP/phase granularity, recorded here so they
are not lost in polish.

1. **Engine refactor must be behaviour-preserving.** No production block declares
   top-level `conditional_rules` today (only `test_engine.py`). Generalising
   `_validate_conditional_rules` into `_apply_conditional_rules(obj, rules,
   path)` must keep the top-level call (`path=""`) and ensure the error path for
   `path=""` yields `origin`, not `.origin` (no leading dot). VT-142-ENGINE-003
   guards "no behaviour change". Engine tests required.
2. **`applies_to.specs` composition.** Derived `applies_to.specs` = union of
   `specs[].spec_id` ∪ `requirements[].destination.spec` ∪
   `requirements[].origin[].ref` (kind=spec). The list-column source/destination
   *split* is recomputed in the domain summary (§7.1), not read back from
   `applies_to`. IP to finalise dedup/order.
3. **~~`unknown` tolerated_alias blast radius.~~ DISSOLVED (P04 consult, DEC-142-09).**
   Synthesis omits the optional `lifecycle` instead of inventing `unknown`, so the
   shared `REQUIREMENT_STATUSES` enum is never touched. R-142-02 no longer applies.
4. **~~Legacy `action: move` without `origin`.~~ RESOLVED (P04 consult).** The corpus
   has **zero** `move` blocks (all 27 are `modify`); synthesis emits `modify`, not
   `move` (DEC-142-09). No record self-inflicts the move→origin strict error. The
   real flip risk was the SPEC-only ID regex (97 legit refs) — addressed by the
   §8.0 pattern broadening (DEC-142-08), not by drift-tracking. VA-142-CORPUS-001
   confirms zero residual.
5. **FM-beside-block strict check is new for revision.** Rejecting FM
   `applies_to`/`source_specs`/etc. alongside a block (VT-142-DERIVE-002) reuses
   the delta check path (DEC-138-10); confirm it generalises to `kind:revision`
   rather than being delta-keyed.

## 14. References

- DR-136 §10 (reconciled placement table) + §11 (migration mechanics)
- DE-136 `notes.md` F-F scope-note; phase-03 §9 erratum record
- DE-118 (REVISION_CHANGE_METADATA foundation)
- DR-138 §6.1 / DEC-138-10 (applies_to derivation precedent)
- DR-141 (audit per-artefact precedent: block/list/migration shape)
- `blocks/revision_metadata.py`, `blocks/metadata/{schema,validator}.py`
- `changes/artifacts.py` (`_derive_applies_to`)
- ADR-002, ADR-008, ADR-010
