---
id: RE-019
slug: refresh_prod_004_frontmatter_metadata_validation_for_memory_and_compaction
name:
  Spec Revision - refresh PROD-004 frontmatter metadata validation for memory
  and compaction
created: "2026-03-03"
updated: "2026-03-03"
status: complete
kind: revision
aliases: []
relations: []
source_specs:
  - PROD-004
destination_specs:
  - PROD-004
---

## 1. Context

- **Why**: `PROD-004` has drifted from implemented frontmatter metadata capabilities and now blocks clear scoping for DE-036 metadata compaction work.
- **Source Specs**: PROD-004
- **Destination Specs**: PROD-004
- **Requirements Affected**:
  - Update existing FR/NF language that still claims "11 kinds/schemas".
  - Add new requirement: `PROD-004.FR-007` for canonical/derived field classification and compaction metadata support.

## 2. Related Artefacts

- **Commits**: none yet
- **Issues / Problems / Improvements**: none linked yet
- **Decisions / ADRs**: none required at this stage
- **Follow-up Deltas**:
  - DE-036 (frontmatter metadata compaction and canonicalization controls)

## 3. Summary of Changes

- **Specs Updated**:
  - `specify/product/PROD-004/PROD-004.md`
- **Requirements**:
  - Keep FR-001..FR-006 intact where still valid.
  - Add `FR-007`: metadata definitions must support canonical/derived/default-aware persistence semantics enabling compaction profiles.
- **Responsibilities / Relations**:
  - Refresh kind/schema counts:
    - registry entries: 17 (`base` + 16 artifact aliases including `phase`/`task`)
    - unique metadata definitions: 15 (with `plan/phase/task` shared)
  - Add explicit memory coverage in kind inventory and appendix table.
  - Remove/replace stale static test-count claims ("275-440 across 11 schemas") with either:
    - current measured values, or
    - non-numeric phrasing tied to CI suites to avoid future drift.
  - Extend capability text to include metadata compaction as a first-class extension of metadata-driven validation.

## 4. Consequences

- Positive:
  - Aligns product spec with implemented system reality (memory kind and current registry).
  - Gives DE-036 a clean normative requirement anchor for compaction behavior.
  - Reduces future drift by avoiding brittle hard-coded counts where possible.
- Negative / Trade-offs:
  - Requires careful wording to avoid double-count confusion (entries vs unique definitions).
  - FR-007 introduces additional schema engine scope that must be implemented and tested.
- Impact on audits/testing:
  - Add/adjust validation and JSON-schema tests for any new `FieldMetadata` properties (e.g., default/persistence).
  - Update PROD-004 verification coverage entries to include FR-007 artifacts.
- Communication:
  - Call out that this is a spec-alignment revision, not an implementation-complete claim.

## 5. Actions

- [x] Update PROD-004 assumptions, capability descriptions, and appendix inventory with current counts and memory kind.
- [x] Add FR-007 requirement text and link it to compaction capability scope.
- [x] Update verification coverage section in PROD-004 for FR-007 planning.
- [x] Add revision reference in DE-036 (`revision_links.introduces: RE-019`) — already present (user pre-linked).
- [x] Run `uv run spec-driver validate` after spec updates.

## 6. Notes

- Observed implementation reality (2026-03-03):
  - `FRONTMATTER_METADATA_REGISTRY` has 17 entries:
    `audit,base,delta,design_revision,issue,memory,phase,plan,policy,problem,prod,requirement,risk,spec,standard,task,verification`
  - Unique metadata definitions are 15 due to shared `PLAN_FRONTMATTER_METADATA` for `plan|phase|task`.
- `FieldMetadata` currently has no explicit `default` or persistence/canonicality fields, confirming FR-007 is a real gap for DE-036.

### Implementation notes (2026-03-03)

- **Done**: All RE-019 actions complete. PROD-004 updated in place.
- **Changes applied to PROD-004**:
  - Kind counts: "11" → "all registered" / "15 unique definitions / 17 registry entries" throughout
  - Memory row added to appendix kinds table
  - Brittle test counts ("275-440") replaced with CI-anchored phrasing
  - New capability: `metadata-compaction` with FR-007 (canonical/derived classification + compaction profiles)
  - VT-007 added to verification coverage block
  - FR-007 added to relationships block primary requirements
  - Phase 6G added to migration timeline for compaction work
  - Change history entry added
- **Surprise**: `accepted` is not a valid delta status for deltas (valid: draft, pending, in-progress, deferred, complete, completed). Set DE-036 to `in-progress` instead.
- **Rough edges**: PROD-004 appendix "Frontmatter Kinds Summary" table was already inconsistent before this revision — it listed Policy/Standard/Risk rows but FR-001's kind list omitted them. Now aligned.
- **Verification**: `uv run spec-driver validate` passes clean.
- **Uncommitted work**: all changes unstaged.
