# Plan: Contract Mirror Tree Index (PROD-014)

Approved: user request in chat on 2026-02-20.

## Goals
- Make the “contract mirror tree index” spec a product spec (`PROD-014`).
- Remove the accidentally-created tech spec (`SPEC-131`) once the content is migrated.

## Steps
1. Migrate the v1 design and requirements from `specify/tech/SPEC-131/SPEC-131.md` into `specify/product/PROD-014/PROD-014.md`, updating requirement IDs to `PROD-014.*`.
2. Delete `specify/tech/SPEC-131/` and the generated tech by-slug entry `specify/tech/by-slug/contract_mirror_tree_index`.
3. Sanity-check that `PROD-014` stands alone (no remaining references to `SPEC-131`).

